# 工作进度

用途：面向中文用户记录项目推进情况。它帮助人快速理解当前进展，但 AI 的操作依据仍以 `docs/codex/STATUS.md`、当前用户要求和 Git 状态为准。

最后更新：2026-08-23 19:13

## 当前摘要

- 当前目标：以正式版本 `1.10.1` 维护独立主线，在实际 3FUI 环境完成模型下载与处理回归。
- 当前进度：版本标识、ModelScope 自有镜像、后端感知环境检查和真实错误提示已完成，最新 EXE/DLL/布局已部署。
- 已完成：NCNN 启动检测不再被无关 TensorRT Engine 阻断；插件当前配置已切换到可用的 NCNN 同名模型；公开模型清单和真实文件下载校验通过。
- 主要风险：TensorRT 必须在 NVIDIA CUDA 环境测试；GitHub Release 尚未创建；模型下载页的单项/批量交互仍需宿主内确认。
- 下一步建议：重启 3FUI 确认启动检测通过，再检查模型页刷新、单项下载、批量下载和已安装状态。

## 详细日志

在本节末尾追加新记录。每条记录使用 `YYYY-MM-DD HH:MM`，方便区分同一天内多次操作的先后顺序。不要覆盖旧记录。

### 2026-08-22 11:13

- 本次目标：修复用户截图中的 LakeUI 混用、开关近方形、按钮异常拉高，以及模型页“当前无网络”。
- 完成内容：把插件可见标签、卡片、进度条、下拉框、复选框和数值滑块统一为 LakeUI；修正 BooleanSwitch 和所有操作按钮尺寸；修正 CLI 在线列表执行顺序和插件错误分类。
- 重要文件：`VideoEnhancerPlugin/PluginPanel.vb`、`VideoEnhancerPlugin/QuadGridForm.vb`、`VideoEnhancerPlugin/QuadGridControls.vb`、`cli/Program.cs`、`videoenhancer.exe`、`videoenhancer.3fui.dll`。
- 验证情况：CLI 和插件均编译成功；在 `core-path` 无效的情况下仍成功读取 82 个 ModelScope 文件；模型文件直链 HTTP 200；EXE 版本为 1.9.5。
- 遗留问题：需要用户实际 3FUI 环境的视觉截图确认；没有执行大型模型下载，以免占用大量磁盘和流量。
- 下一步：备份旧产物后替换 EXE/DLL，重启 3FUI 测试；若仍有局部控件间距不满意，基于新截图继续微调。

### 2026-08-22 12:46

- 本次目标：停止继续维护本地旧 UI，直接采用原作者远端刚合并的 UI 改版，制作隔离的抢先体验包。
- 完成内容：从远端 HEAD `220ebb4` 构建，其中 UI 贡献提交为 `a95bdfe`；重新合入 ModelScope 真网络错误识别、列表执行顺序、超时和详细提示；版本标记为 `1.9.6-preview.1`。
- 交付产物：`preview/1.9.6-preview.1/VideoEnhancer-1.9.6-preview.1-win-x64.zip`，稳定版根目录 v1.9.5 未覆盖。
- 验证情况：插件和 CLI 均构建成功；EXE 版本正确；在线模型列表读取到 82 项；ZIP 清单及 SHA-256 已检查。
- 已知问题：远端清理提交删掉了仍被代码引用的嵌入载荷，预览构建已从本地已验证产物恢复；尚待真实 3FUI 宿主视觉回归。
- 下一步：用户备份现有 EXE/DLL 后安装预览包，反馈新 UI 截图和模型下载页表现。

### 2026-08-22 13:01

- 本次目标：把抢先体验版正式升为当前主线，并查清 TensorRT 与超分/补帧组合的真实限制。
- 主线切换：根目录 `videoenhancer.exe`、插件 DLL 和布局文件已替换为 v1.9.6-preview.1，v1.9.5 退役。
- TensorRT 结论：当前插件只列出 `.engine`，运行时只做兼容性验证，转换页需要手动选择 `.pth`；不同显卡或运行库下不兼容时只会提示重编译，没有自动构建和回退。
- 组合处理结论：互斥判断只存在于插件开关；CLI 可同时传放大和 RIFE 模型，官方 RVE 后端也会在同一管线中先补帧后超分，因此不是模型限制。
- 额外问题：当前 TensorRT 补帧会把 NCNN RIFE 目录交给 TensorRT 后端，存在格式错配，不能只粗暴删除互斥判断。
- 下一步：设计本机 Engine 缓存键、自动编译进度/取消和失败回退；随后加入后端能力矩阵、两个独立后端与处理顺序选项。

### 2026-08-22 13:09

- 追加问题：3FUI 停留在视频超分页时最小化，再从任务栏恢复，会短暂显示壁纸和黑色矩形骨架。
- 检查结论：3FUI 毛玻璃模式会主动把插件的 `ModernPanel1` 设为透明并映射主窗体背景；插件内部又使用了大量透明容器，且没有整页双缓冲或恢复重绘处理，因此恢复时会把逐层绘制过程直接展示出来。
- 影响范围：纯 UI 绘制问题，不会触发模型、后端或网络加载，也不影响任务数据。
- 快速验证：临时关闭毛玻璃/自定义背景后重复最小化与恢复，现象应消失或明显缩短。
- 修复方向：保留个性化背景，给插件根控件增加不透明兜底，保存背景面板引用，并在主窗体恢复时统一刷新；实测后再决定是否减少透明布局层级。

### 2026-08-22 14:11

- 本次目标：把 TensorRT 防呆、超分/补帧组合顺序和窗口恢复闪屏作为三个独立提交完成，并发布新的主线抢先版。
- TensorRT：选择 PTH 后，如果本机缓存不存在会自动编译；缓存文件名包含 GPU、TensorRT 版本、输入尺寸和源模型摘要。缓存命中前会验证，不兼容时自动重建。
- 组合处理：超分和补帧不再互斥。默认是“画质优先：先超分，再补帧。”；也可以选择“速度/算力优先：先补帧，再超分。”。需要分阶段时使用 FFV1 无损中间视频并自动清理。
- 后端防呆：TensorRT、ONNX、FlashVSR 超分与补帧组合时，补帧自动走 NCNN RIFE；CUDA 组合使用 CUDA RIFE，不再把 NCNN 模型错误交给 TensorRT。
- 恢复闪屏：插件根面板现在是 3FUI 可直接识别的稳定字段，增加 `#181818` 兜底底色、双缓冲和从最小化恢复时的统一重绘。
- 提交记录：`762cabb`（TensorRT 自动构建）、`ce75515`（两种处理顺序）、`f774532`（恢复重绘）；另有 `956be92` 抢先版基线提交。
- 验证情况：CLI/插件编译通过；TensorRT 首次构建与缓存命中伪后端测试通过；五阶段组合管线断言通过；FFV1 实际编码通过；插件布局和恢复状态测试通过；在线模型列表仍为 82 项。
- 交付产物：`preview/1.9.6-preview.2/VideoEnhancer-1.9.6-preview.2-win-x64.zip`，根目录当前运行产物也已升级到 v1.9.6-preview.2。
- 待实机确认：真实 NVIDIA TensorRT 大模型编译/推理，以及完整 3FUI 毛玻璃环境的最小化恢复视觉效果。

### 2026-08-22 15:30

- 修复模型下载目录：`Bin/ffmpeg.7z` 现在下载到核心目录的 `bin`，不再落到 `models`；程序也会尝试迁移 preview.2 旧位置的 FFmpeg。
- 修复组合处理路径：同一后端的“先超分，再补帧”改为同一 RVE 进程内逐帧传递，最终只执行一次用户选择的编码器，不再生成整段中间视频。
- 跨后端组合仍使用无损 FFV1 中间视频，普通视频至少使用 10-bit `gbrp10le`；检测到 PQ/HLG HDR 时使用 16-bit `gbrp16le` 并启用 HDR 模式，避免降位深。
- CLI 已重新编译发布，preview.2 ZIP 和根目录 EXE 已更新；ModelScope 在线列表 82 项，ZIP 清单 4 个文件。
- 待实机确认：完整 RVE Python 依赖、NVIDIA 环境下的逐帧顺序/HDR 回归，以及跨后端中间文件清理。当前源码仓库本次修复尚未提交，切换设备或工具前建议先提交。

### 2026-08-22 15:50

- 处理顺序下拉框已改为响应式百分比布局，组合使用 `DockStyle.Fill`、`AutoSize=False`、最小高度和边距，减少长文本溢出并与其他控件保持一致。
- 下载行为已纠正：不同资源可以同时下载；“下载全部”最多同时运行 3 个文件，并采用滑动补位，任一文件完成后立即启动下一个，不等待整批结束。
- 相同资源路径不会重复启动；清理下载压缩包会等待所有下载完成，避免删除正在使用的归档。
- 插件 DLL、CLI、preview.2 ZIP 和根目录运行产物已重新编译更新。当前源码仓库本次修复尚未提交，建议实机确认后提交。

### 2026-08-22 17:10

- 按用户确认，转场阈值恢复使用 RVE 官方外部值，不再在插件层转换成 0-1。
- UI 预设为 `1.0 / 2.0 / 3.5 / 4.0 / 6.0 / 8.0 / 10.0`，默认 `4.0`；CLI 接受官方 `0-10` 外部标尺并直接透传给 RVE。
- 已重新编译插件和 CLI，更新 preview.2 的 EXE、DLL、说明和 ZIP，同时同步根目录运行产物。
- 验证：构建 0 错误（保留 2 个既有 CA1416 警告）；非法 `10.1` 会拒绝；在线模型列表 82 项；ZIP 四项清单和哈希已复核。
- 当前源码仓库仍有未提交修改，实机 RVE/GPU 和完整 3FUI 视觉回归待确认。

### 2026-08-22 17:25

- 已核对 RVE 官方 `--tilesize`：它表示输入帧分块边长，不是固定块数量；`0` 是 RVE 默认整帧处理，不会根据显存自动试探。
- 超分分块控件改为“RVE 默认（0）”，并保留 128/256/384/512/768/1024 px 常用预设。显式分块仅对 NCNN、CUDA/PyTorch、TensorRT 生效，ONNX/FlashVSR 不使用。
- TensorRT 缓存键加入分块尺寸，避免整帧 Engine 与分块 Engine 错误复用；CLI 对不支持后端的显式分块直接报错。
- CLI、插件和 preview.2 发布包已重新构建同步。构建 0 错误、2 个既有 CA1416 警告；ONNX 显式分块拒绝检查通过。
- canonical 源码当前 `main...origin/main [ahead 4, behind 2]`，本地改动尚未提交；建议审核后提交，再处理远端提交并推送。
### 2026-08-22 17:30

- 应用户要求梳理了插件对 LakeUI 的使用方式：插件不改 LakeUI 源码，而是引用 3FUI 宿主的 LakeUI.dll，在 `PluginPanel.vb` 里创建 ModernPanel、ModernButton、ModernComboBox、ModernTextBox、ModernTabControl、HtmlColorLabel、BooleanSwitch 等控件，并通过属性统一配色、圆角、边框和字体。
- 卡片 `FluentCardPanel` 和进度条 `FluentProgressBar` 是插件在 `QuadGridControls.vb` 里自绘的同名类，并非 LakeUI 控件；页面骨架仍是原生 TableLayoutPanel/FlowLayoutPanel。
- 排版差的根源：各页面全部是手写绝对像素布局（主页面固定总高 586、绝对列宽、行高不随 DPI 缩放），窗口偏高时下方留白，窗口偏窄或 150% DPI 时控件容易挤压错位；说明文字混用原生 Label 与 LakeUI 标签，观感也不一致。
- 本次未修改任何源码；下一步等用户指明具体页面或提供截图后逐页调整布局。canonical 源码仍有未提交改动（ahead 4、behind 2），建议先提交再动布局。
### 2026-08-22 17:45

- 已把此前积压的 RVE/RIFE 改动全部提交进 canonical 源码仓库：提交号 `aaa32b5`，共 7 个文件（+740/−158），覆盖 RIFE 独立后端、转场阈值、动态光流、超分分块、TensorRT 缓存键、模型下载并发与 `bin` 目录迁移、同后端帧级组合管线和 FFV1 中间格式。
- 提交前逐文件审核了差异：无调试遗留、无空白错误；少量行仅行尾不同（混合 CRLF/LF），不影响编译，未做改写。
- 因多组功能在同一批文件中交错，合并为一个提交并在提交说明里分条列明，避免拆出无法独立构建的中间提交。
- 当前 canonical 工作树干净；本地 `main` 领先远端 5 个提交、落后 2 个。下次推送前需先 `git pull` 合并远端 2 个提交，如有冲突再处理。

### 2026-08-22 17:37

- 根据截图修复超分工作台布局，保留全部 LakeUI 交互控件，不替换为 WinForms 原生下拉框。
- 补帧后端、转场阈值、补帧倍率等 `ModernComboBox` 统一使用 `AutoSize=False`、`Dock=Fill`、最小高度和 Overlay 下拉模式，避免右侧箭头被父布局裁掉。
- 补帧参数列改为明确比例，组合处理顺序下拉框行高调整为与普通视频处理下拉框一致。
- 工作台根布局改为顶部停靠的真实内容高度，并配合页面纵向滚动，小窗口底部图片增强区域不再直接消失。
- CLI 和插件构建通过；当前未在完整 3FUI 宿主做视觉回归。canonical 源码仓库 `main` 本地领先远端 5 个提交、落后 2 个提交，工作树有未提交布局修改，建议审核后提交。

### 2026-08-22 17:43

- 根据小窗/最大化对比截图继续修复：工作台根布局增加 12px 右侧安全内边距，避免滚动条边界遮挡补帧后端、转场阈值和补帧倍率的箭头。
- “组合处理顺序”整体下移 8px，并在 LakeUI 下拉框切换为不可编辑后重新同步选中项，恢复中间框中的“画质优先：先超分，再补帧”文字。
- CLI 构建 0 错误、插件 DLL 构建成功；需替换最新 DLL 后复测小窗口。

### 2026-08-22 17:50

- 根据用户要求调整布局策略：视频超分保持在上方，运动补帧整体移动到下方，两个模块不再左右并排争用宽度。
- 补帧后端、模型、倍率、转场阈值和动态光流现在使用整行宽度；小窗口只增加纵向滚动，不再通过压缩右侧列来显示。
- CLI 构建 0 错误，插件 DLL 构建成功。最新 DLL 位于 `preview/1.9.6-preview.1/src/videoenhancer.3fui.dll`，需替换后进行实机确认。

### 2026-08-22 18:05

- 针对纵向滚动条白色、与深色界面不协调的问题，为工作台 AutoScroll 生成的滚动条应用 Windows 深色主题和深色背景。
- 滚动逻辑保持不变，也没有替换任何 LakeUI 交互控件；在布局完成和窗口尺寸变化后都会重新应用样式。
- CLI 和插件构建通过，需替换最新 DLL 后确认宿主是否覆盖滚动条主题。

### 2026-08-22 18:30

- 滚动条改为工作台黑色自绘覆盖层，轨道黑色、滑块深灰色，支持拖动，并根据实际内容高度自动显示/隐藏。
- “组合处理顺序”现在只有插件总开关、视频超分、运动补帧三个开关同时开启时才可用；任意一个关闭都会变灰。
- CLI 和插件构建通过，最新 DLL 已生成，需替换后在 3FUI 中确认。

### 2026-08-22 18:45

- 修复组合处理顺序初始化后仍为空的问题：句柄创建后延迟同步 LakeUI 下拉框，临时写入 `Text` 后恢复不可编辑，并强制刷新。
- CLI 和插件构建通过，最新 DLL 已生成，需替换后确认初始化显示。

### 2026-08-22 19:00

- 已将超分/补帧组合处理行为写入插件内置“使用教程”。
- 教程新增同后端与跨后端说明：同后端单进程逐帧处理；先超后补使用帧传递包装器；跨后端使用 FFV1 无损中间视频。
- 同时说明 SDR `gbrp10le`、PQ/HLG HDR `gbrp16le`、音频字幕复制、临时文件自动清理和磁盘空间要求。
- 插件构建通过，最新 DLL 已生成。

### 2026-08-22 19:15

- 已确认 3FUI/LakeUI 提供公开的 `V3_ScrollBarRenderer`，性能监控页使用的就是这套滚动条布局计算。
- 工作台黑色滚动条覆盖层现已复用 `V3_ScrollBarRenderer` 的轨道/滑块几何计算，不再自定义滑块比例；仅保留黑色主题绘制。
- 插件构建通过，最新 DLL 已生成。

### 2026-08-22 19:35

- 检查 3FUI/LakeUI 性能模式后，确认插件首屏主要同步创建了两个 `WebBrowser` 教程页，并且选项卡构造未批量暂停布局。
- 教程页改为首次点击“模型指南/使用教程”时才初始化浏览器引擎；选项卡构造增加 `SuspendLayout/ResumeLayout`，减少首次渲染重复测量和重绘。
- 插件构建通过，最新 DLL 已生成。暂未在完整宿主中做毫秒级性能基准。

### 2026-08-22 19:55

- 根据实机截图确认白色滚动条位于工作台非客户区，原覆盖层无法遮挡；现在直接隐藏真实系统滚动条，保留基于 `LakeUI.V3_ScrollBarRenderer` 的黑色滚动条覆盖层。
- 组合处理顺序继续修复显示缓存问题，最终保留 LakeUI 可绘制文本模式，确保初始化状态文字可见。
- 教程浏览器延迟初始化和选项卡批量布局优化仍保留；插件构建通过，需替换最新 DLL 后确认。

### 2026-08-22 20:15

- 根据滚动后白色系统条复现的问题，在工作台滚动事件中立即及延迟再次隐藏真实系统滚动条。
- 组合处理顺序新增 LakeUI 文本显示层，直接显示配置状态；原 `ModernComboBox` 继续负责箭头和下拉选择。
- 插件构建通过，需替换最新 DLL 后重点复测滚动前后滚动条和组合文字。

### 2026-08-22 20:40

- 重新核对 LakeUI 3.22.0 的实际 API：`ModernPanel` 自带 `V3_ScrollBarRenderer` 和完整滚动属性，正确方式不是给 WinForms `Panel.AutoScroll` 叠加覆盖层。
- 工作台已改为真正的 LakeUI `ModernPanel` 原生垂直滚动，删除系统滚动条主题、P/Invoke、自绘覆盖条和组合文字覆盖层。
- 组合处理顺序改为通过真实 `SelectedIndex` 变化更新 LakeUI 内部文本渲染器。
- 运行时实例化检查确认：工作台为 `ModernPanel/Vertical/AutoScroll=False`，组合框的索引、选中项和文字均为“画质优先：先超分，再补帧”。插件构建通过。

### 2026-08-22 18:18

- 针对组合处理顺序只有箭头、没有选中文本的问题，初始化和状态同步时同时设置 LakeUI 下拉框的 `SelectedIndex`、`Text` 并强制刷新。
- 针对白色滚动条，改为枚举工作台真实子窗口句柄，匹配系统 `ScrollBar` 后应用深色主题，不再只查找 WinForms 控件树。
- CLI 和插件构建通过，需替换最新 DLL 后在 3FUI 中确认最终显示。

### 2026-08-22 18:53

- 模型下载页已从 WinForms `FlowLayoutPanel.AutoScroll` 迁移到 LakeUI 3.22.0 的 `ModernPanel` 原生流布局和垂直滚动条，深色轨道、滑块和滚动步长均由 LakeUI 实际 API 管理。
- 下载分组内部也改用 LakeUI 流布局；资源列表加载时批量暂停布局、完成后一次性提交，滚动面使用不透明背景，避免透明控件在拖动时留下旧画面。
- 找到窗口状态切换重绘明显的直接原因：插件额外启用了 `ResizeRedraw`，并在恢复窗口后强制递归刷新、同步更新整棵控件树。该路径已删除，只保留双缓冲和正常的 LakeUI/WinForms 消息绘制。
- CLI 和插件构建通过，运行时确认下载列表确实为 `ModernPanel/Flow/Vertical/AutoScroll=False`，顶层 `ResizeRedraw=False`。仍需在完整 3FUI 中拖动滚动条并测试最大化、还原和最小化恢复的最终观感。

### 2026-08-22 19:03

- 已确认插件并非没有接入 LakeUI DPI：插件继承宿主缩放，选项卡使用 LakeUI 的 DPI 模式，LakeUI 控件会自动响应 DPI 变化；同一显示器内最大化/还原本身也不会改变 DPI。
- 真正差异是插件根面板此前会被3FUI绑定到动态背景穿透。窗口尺寸改变时，LakeUI 需要重建整张背景缓存；插件又额外对多层控件开启 WinForms 双缓冲并整页重算行高，因此出现原生参数页没有的整页重绘感。
- 插件现改为独立、不透明的 LakeUI 深色画布，取消动态背景穿透和手工双缓冲；工作台固定内容布局，仅由 LakeUI 原生滚动改变视口，不再在最大化/小窗切换时整套重排行高。
- 插件和 CLI 构建通过，运行时确认 DPI 模式仍有效、背景源为空、页面颜色为 `#181818`。需替换最新 DLL 后验证最大化、还原和最小化恢复动画。

### 2026-08-22 19:16

- 根据新截图重新区分“控件重建”和“布局中间帧”。反编译3FUI宿主后确认，插件面板只创建并绑定一次，窗口 Resize 不会重新执行插件入口。
- 宽窗和小窗切换测试也确认：插件面板、工作台根布局、后端下拉框的对象和窗口句柄全程不变。截图实际是 LakeUI 滚动视口先缩小、内部表格稍后才采用新宽度，导致右列短暂留在旧位置。
- 现在尺寸变化会在同一个消息内更新既有工作台根表格宽度并完成布局，不会清空、移除或重新添加任何控件，也不会重新计算全部行高。
- 插件构建通过；模拟 1600x900 → 1000x700 → 1600x900 时，页面和根布局宽度立即同步，未等待下一轮消息。需用最新 DLL 在3FUI中确认截图所示中间帧消失。

### 2026-08-22 19:43

- 模型下载页已按 LakeUI 作者建议改为 3.22.0 `UltraDetailListView`，不再为每个模型创建独立面板、标签和按钮。
- 真实在线清单包含 82 个资源：现在显示为 8 个分组和 90 个绘制项，列表内部子控件为 0；旧结构同规模需要创建 302 个后代控件，因此窗口缩放和滚动时不再逐个重排这些资源控件。
- 单文件与“下载本组”继续支持并行下载，总并发严格最多 3 个；任一文件完成后立即启动队列中的下一个，不等待一批全部结束。
- CLI 与插件构建通过，真实模型清单渲染、并发上限、列表对象和窗口句柄稳定性均已验证。最新 DLL 位于 `preview/1.9.6-preview.1/src/videoenhancer.3fui.dll`，仍需在完整 3FUI 中确认最终视觉和鼠标交互。

### 2026-08-22 19:54

- 重新实测超分工作台缩放：旧路径一次窗口切换会触发 61 次布局和约 362-404 次绘制，确实存在明显重复回画，并非控件重建。
- 删除了尺寸变化时手工修改根表宽度并强制重排整页的逻辑，改为固定内容高度和左右锚定；透明布局层改为同色不透明画布，视觉颜色不变。
- 优化后相同测试为 58 次布局、22-28 次绘制，Paint 数量约减少 93%；工作台对象和窗口句柄保持不变，小窗下下拉框与 LakeUI 纵向滚动尺寸正常。
- 同时修复关闭诊断窗体时的 `Cannot access a disposed object: LakeUI.ModernPanel`：销毁插件前先解除 LakeUI 标签页绑定，复测不再出现未处理异常。
- CLI 和插件构建通过。最新 DLL 位于 `preview/1.9.6-preview.1/src/videoenhancer.3fui.dll`，需在真实 3FUI 中确认最大化/还原的最终观感。

### 2026-08-22 20:37

- 直接检出并研究 FFmpegFreeUI 6.1.39 官方源码，确认原生参数页使用 `ModernPanel1` 提供背景穿透和垂直滚动，字段直接挂在根面板或浅层普通 Panel 中，不使用多层百分比表格。
- 恢复插件个性化背景：宿主背景可以从控件间隙透出，不再使用整页不透明 `#181818` 遮挡；超分工作台恢复 LakeUI 黑色滚动条。
- 视频超分和运动补帧区域删除多层透明布局父链。相同缩放测试从旧版约 362-404 次 Paint 降至 31-45 次；官方质量页同条件为 16-20 次，已经接近原生量级。
- 930px 小窗下补帧后端、模型、倍率、转场阈值等 11 个下拉框最小尺寸为 `207x36`，箭头完整；组合处理顺序初始化文字正常显示。
- 彩色背景宿主截图确认背景和滚动条实际可见；关闭页面不再触发 `LakeUI.ModernPanel disposed` 异常。CLI 与插件构建通过，最新 DLL 位于 `preview/1.9.6-preview.1/src/videoenhancer.3fui.dll`。
- 仍需在真实 3FUI 中连续测试最大化/还原、拖动滚动条和模型列表操作。源码仓库当前 `ahead 5, behind 2` 且有未提交修改，建议确认视觉效果后先提交，再处理远端分叉。

### 2026-08-22 20:44

- 实机窗口缩放和滚动已经流畅，但核对发现 3FUI 当前加载的仍是 19:53 旧 DLL，并不是 20:38 生成的 LakeUI 原生滚动条版本。
- 旧安装文件与最新构建哈希不一致；覆盖时因 `FFmpegFreeUI.exe` 正在占用 DLL 而失败。新版已暂存为插件目录下的 `videoenhancer.3fui.dll.new`。
- 完全退出 3FUI 后需要完成原位替换并核对哈希，再判断滚动条显示；当前不应继续按旧 DLL 的画面修改代码。

### 2026-08-22 20:47

- 用户退出 3FUI 后，最新插件 DLL 已成功覆盖到实际插件目录；源文件与安装文件 SHA-256 完全一致，确认不再是 19:53 的旧版本。
- 可以重新启动 3FUI 复测超分工作台右侧 LakeUI 黑色滚动条。目录中的 `.dll.new` 暂存文件即使保留也不会被宿主加载，不影响测试。

### 2026-08-22 20:50

- 找到三个开关变成大圆的原因：新横向布局器把 BooleanSwitch 强制拉伸到整列，覆盖了原来的 DPI 胶囊尺寸。
- 布局器现会保留定尺寸控件并居中；小窗和宽窗运行时验证三个开关均为 `38x20`，下拉框、按钮和滚动布局不受影响。
- 最新 DLL 已直接覆盖到实际 3FUI 插件目录，安装文件与构建输出哈希一致。重新启动 3FUI 后即可检查胶囊开关样式。

### 2026-08-22 21:07

- 定位到“先超分、再补帧”崩溃原因：2x NCNN 超分已经产生 `3840x2160` RGB24 字节，但返回 Frame 仍标记为源视频 `1920x1080`；包装器随后在超分后执行转场检测，按错误尺寸解析数据，正好产生 4 倍数组大小不匹配。
- RVE 原生流程在超分前做转场检测，因此不会遇到这个错误。推荐在插件包装器中先检测源帧、再超分和补帧，并补充 NCNN 返回帧尺寸校正；无需先修改另一个项目。
- 本轮仅完成诊断，尚未改代码。当前渲染线程已经退出，任务应停止后等待修复再重试。
- 另确认当前 RVE 直接帧管线读取的是 `rgb24`，最终输出设置 10-bit 不代表内部推理保留了 10-bit；真正的 8/10/12-bit 自动传递仍是独立问题。

### 2026-08-22 21:35

- 已修复 NCNN“先超分、再补帧”把 4K 帧按 1080p 尺寸解析导致的崩溃：转场检测改在源帧上执行，超分后的帧尺寸会在送入 RIFE 前校正。
- 后台渲染线程异常现在会明确让任务失败；即使已经产生半截输出，也不会再误报处理成功。内置包装器按文件哈希更新，避免同长度旧脚本继续被缓存。
- 6 项帧管线测试通过；使用本机 NCNN 模型实测仅超分、仅补帧、先补后超、先超后补四条短片路径均成功，尺寸、平均帧率和帧数正确。
- 已把最新 EXE 和 DLL 直接覆盖到 `C:\Program portable\3FUI\plugin`，源文件与安装文件 SHA-256 完全一致，用户将使用原视频继续实测。
- 位深边界已写入教程：当前 RVE 的 SDR 内部仍是 8-bit RGB24，10-bit 最终编码不等于 10-bit 推理；HDR 计划只允许 CUDA/PyTorch 或 TensorRT。合成 PQ 样本本轮暴露出 HDR 元数据探测漏判，尚待后续修复，不能宣称 HDR 检查已完成。

### 2026-08-22 21:46

- 今天的修改已整理为 7 个待提交方向：TensorRT 自动缓存、组合处理顺序、RIFE/转场/分块参数、LakeUI 页面性能与布局、模型下载列表与 3 并发队列、`upscale-first` 帧尺寸和异常传播修复、HDR/位深能力门禁；全部合并后再单独做发布 PR。
- 当前本地分支基于旧共同基线，远端已经合并 1.4，双方都修改了核心 UI 和 CLI 文件，因此不能把现有分支整体直接提交。后续应从最新远端分支逐个重放，每个 PR 独立构建和验证。
- 旧预览基线提交不单独发 PR；早期强制重绘方案已被最终 LakeUI 官方布局替代，也不原样提交。生成的 EXE/DLL、Python `__pycache__` 不进入功能 PR。
- HDR 后端限制暂列在帧管线修复 PR 的 Draft 范围，必须先修复 PQ/HLG 元数据漏判并增加自动测试，避免提交一段实际无法稳定触发的保护逻辑。

### 2026-08-22 22:30

- 已按功能实际创建并推送 7 个 PR：#3 TensorRT 自动缓存、#4 组合处理顺序、#5 RIFE 后端与参数、#6 LakeUI 工作台性能、#7 模型下载列表与最多 3 路连续并发、#8 先超后补帧管线可靠性、#9 HDR 检测与位深门禁（Draft）。
- #7 已确认模型下载不会再把 FFmpeg 作为普通模型误放到 `models`；启动时会把旧版 `models\ffmpeg` 兼容迁移到 `bin\ffmpeg`。下载全部时最多同时运行 3 个文件，任一完成立即补下一个。
- #8 已通过 6 项帧管线测试、插件和 CLI 构建；修复了 NCNN 超分后 Frame 尺寸元数据错误导致的 reshape 崩溃，并让后台线程异常不再被半截输出掩盖。
- #9 已改用 ffprobe 结构化元数据识别 PQ/HLG，但本机合成样本的编码器写出了 `unknown` 传递函数，真实 HDR 文件仍需人工实测，因此保持 Draft。普通 SDR 10-bit 仍不能宣称已实现源位深原样推理。
- 所有 PR worktree 已 clean；主源码 worktree 仍有用户原始未提交修改，没有执行清理或回滚。建议按 #3 -> #8 顺序审查合并，#9 待真实 HDR 验证后再决定。

### 2026-08-23 10:30

- 修复另一台 3060 使用 TensorRT 时环境检查误报“模型库缺失”的问题。原检查逻辑固定寻找 NCNN 的 `.param/.bin` 文件夹，现改为按当前后端检查：TensorRT 使用 `.engine/.pth`，CUDA 使用 `.pth`，ONNX 使用 `.onnx`，NCNN 使用 `.param/.bin`，未指定后端时任一模型族可用即可通过。
- 插件环境检查现在会把当前超分后端传给 CLI，错误信息也会明确显示实际后端。临时模型目录回归确认 TensorRT、CUDA、NCNN 三种列表分别识别正确。
- 已创建 PR #10：`https://github.com/user-Wing/VideoEnhancer/pull/10`，标题为“修复 TensorRT 环境检查误报模型缺失”。CLI 和插件构建通过，保留 2 个既有 CA1416 警告；PR worktree clean，主源码 worktree 仍保留用户未提交修改。

### 2026-08-23 00:40

- 发现实际 3FUI 目录中的 `videoenhancer.exe` 只有约 162 KB，确认这是普通构建输出，不是可直接运行的自包含 CLI。
- 按用户要求暂停 PR 操作，使用主线当前源码重新发布自包含 CLI；发布结果约 16.7 MB，并复制到 `C:\Program portable\3FUI\plugin`。
- 同步复制主线插件 DLL 和布局 JSON。三个文件均已完成源/目标 SHA-256 校验，目标 EXE 为 `16681479` 字节，DLL 为 `4550656` 字节。
- 未修改、清理或提交主线已有的用户改动，下一步由用户在实际 3FUI 中测试。

### 2026-08-23 00:50

- 复核后确认：这次复制的是正确的自包含 EXE，但主线 `RunCheck()` 仍按 NCNN 的 `.param/.bin` 检查模型，尚未包含 TensorRT 后端模型检查修复。
- 用户另一台机器日志中的 `C:\Program portable\3FUI\3FUI\Plugin\models` 多了一层 `3FUI`，还需检查该机的 `core-path` 配置。
- 本轮没有提交源码、创建 PR 或再次复制其他分支产物。

### 2026-08-23 01:05

- 已将 TensorRT 后端模型检查修复直接集成到主线源码，未创建 PR。
- 重新构建并复制主线 DLL、完整自包含 EXE 和布局文件到 `C:\Program portable\3FUI\plugin`。
- 本机验证 TensorRT 检查能识别 `44 个可用模型（tensorrt）`；随后出现的是 AMD 主机无 CUDA GPU 导致的 Engine 不兼容，不再是模型库缺失。
- 主线源码仍未提交，待用户在 3060 机器复测后再决定提交。

### 2026-08-23 01:15

- 复核 PR #10 后确认：逻辑本身有效，但原 PR 基于包含 `BasicVSR++` 和 `core-path` 的另一套基线，不能直接作为当前主线补丁合并。
- 当前主线已完成兼容性集成并验证 TensorRT 模型识别；PR #10 原分支暂不直接合并，待实机确认后再整理提交。

### 2026-08-23 01:40

- 完成项目目录收尾：根目录作为唯一活动主线，旧源码、旧构建物和 PR worktree 统一归档到 `archive/2026-08-23-before-root-mainline`，空的 `preview` 目录不再承载源码。
- 已确认根目录与 EXE 同目录时不需要 `videoenhancer.ini`；删除该本地路径配置，避免在其他机器上拼出重复的 `3FUI\\3FUI\\Plugin` 路径。
- HandShake 状态记录已同步更新；整理提交不包含生成的 EXE、DLL 或 Python 缓存。后续开发直接从根目录 `main` 继续。

### 2026-08-23 12:30

- 评估了将模型下载源迁移到用户自有 ModelScope 仓库的方案：当前下载器只硬编码远端 tree/resolve 地址，目录映射、文件大小和 SHA-256 校验均可复用，后续可做成可配置镜像。
- 暂未搬运或修改源码。原仓库及第三方模型的再分发许可证需要先人工核实；确认自有仓库和 fallback 策略后再实施。
- 本轮 `git pull` 产生的合并冲突已撤销，当前工作树 clean；本地 `main` 相对远端 `ahead 9, behind 4`，后续正式开发前建议先处理分叉并提交整理记录。

### 2026-08-23 12:45

- 已确定项目转为独立发行线：GitHub 负责源码和 Release，ModelScope 负责模型与大文件，仅按需同步上游功能，不再依赖作者在 QQ 群发布版本。
- 后续准备实现可配置模型源、迁移清单与校验、GitHub Release 流程和上游同步记录；本轮未上传模型、未访问 QQ 资源、未修改业务源码。
- 许可证未知的模型先使用私有镜像或占位配置，公开发行只纳入已确认可再分发的资源。

### 2026-08-23 13:30

- 完成对上游 ModelScope 模型清单的初步授权审计：API 返回 118 项，当前插件实际可下载 97 个文件，约 13.56 GiB。
- 上游仓库只有根 README 和 FlashVSR README，没有逐文件许可证、NOTICE 或来源映射。根 README 的 Apache-2.0 不能自动覆盖 PTH、ONNX、TensorRT Engine、压缩包、Python 环境和第三方软件。
- 暂列为“待授权核实”的范围：全部 97 个可下载文件，重点包括 `Backend/*.7z`、`Param-Bin/NCNN-20260821.7z`、`RIFE/RIFE.7z`、`TensorRT-Default/*.engine`、`ONNX/*.onnx`、`PTH/*.pth`、`Bin/*.7z` 及 FlashVSR 权重；仓库中的 `PotPlayer.7z` 另属高风险商业软件。

### 2026-08-23 13:20

- 已确认不把 `PotPlayer.7z` 同步到独立项目。
- 后续继续推进自有模型源和 GitHub Release 的技术改造；授权未知的第三方资源不纳入公开自动发布，保留私有镜像或手动导入路径。

### 2026-08-23 13:25

- 用户决定先使用私有 ModelScope 库暂存资源，不公开发布；`PotPlayer.7z` 继续排除。
- 等待私有数据集 ID。Token 不写入仓库或聊天，上传时由用户在本机环境变量提供；待处理资源约 13.56 GiB。

### 2026-08-23 13:30

- 已创建私有 ModelScope 数据集 `AerithDream/VideoEnhancer-Models`，并从上游直接镜像到本机临时目录后上传。
- 共上传 108 个文件、约 13.5 GiB，排除 `PotPlayer.7z`；ModelScope 报告 108/108 提交成功，0 失败。
- 远端元数据核对通过：目标比上游少且只少 `PotPlayer.7z`，其余文件的路径、大小和 SHA-256 一致；根 README 下载验证成功，目标仓库状态为 private。
- 下一步是把插件 CLI 的 ModelScope 地址改为 `AerithDream/VideoEnhancer-Models`，再构建验证模型下载页。

### 2026-08-23 18:59

- CLI 模型源已切换到 `AerithDream/VideoEnhancer-Models`，也可用环境变量覆盖仓库；保留私库令牌支持，但不会把令牌写进项目或 aria2 命令行。
- 上游目前只新增合并了 PR #9 和 #10：后端模型检查本地已有，HDR 提交夹带删配置、删 UI 和大范围格式改动，因此暂不整体合并，只保留结构化 HDR 探测作为后续候选。
- 用户已将测试集暂时公开。无令牌读取到 82 个下载项，真实 ONNX 下载、大小和 SHA-256 校验通过；CLI 与插件均构建成功。
- 最新 `videoenhancer.exe`、`videoenhancer.3fui.dll` 和布局已部署到 `C:\Program portable\3FUI\plugin`，文件哈希与构建物一致。下一步在 3FUI 模型下载页检查刷新、单项/批量下载和已安装状态。
- 当前源码改动尚未提交；本地分支相对上游 `ahead 9, behind 4`，建议界面确认后提交，再继续下一阶段。

### 2026-08-23 19:13

- 修复启动时反复显示 preview.2 和环境检测失败：正式版本现统一为 1.10.1。
- 环境检查现在只严格验证当前选择的后端，不再让 NCNN/ONNX 等后端被目录中的无关 TensorRT Engine 拖失败；插件失败时会显示真实缺失项，不再只显示第一行版本号。
- 当前机器没有 CUDA，但插件配置曾选择 TensorRT；现已切换到 NCNN 及对应的同名 Param-Bin 模型，其他处理设置未改。
- 最新 EXE/DLL/布局已重新部署。实际配置执行环境检查识别 22 个 NCNN 模型并全部通过；显式选择 TensorRT 时仍会正确提示无 CUDA。
- 版本记录已将 1.10.1 设为正式独立维护主线，preview.2 归档；尚未创建 GitHub Release。重启 3FUI 后即可确认启动提示。

### 2026-08-23 20:08

- 已审查作者 v1.4.2 测试版，只合并了新版模型能力：补帧统一使用 `models\Frame-Interpolation`，新增 GIMM-VFI、GMFSS、CUDA 多格式权重、TensorRT Engine，以及 BasicVSR++ 的 1x 优化目录格式。
- 旧 `models\RIFE` 仍可读取，不会让已有模型和配置立即失效；作者测试版中硬编码原仓库、删除 `core-path`、退回旧环境检查和改成 1.4.2 版本号的部分均未采用，当前正式版本保持 1.10.1。
- 模型下载页已增加 `BasicVSR++`、`Frame-Interpolation` 分类和逐包安装标记。3 个新版补帧包共约 739 MiB，已同步到公开测试镜像；仓库可见性未改，旧 RIFE 下载地址也保留可用。
- CLI 与插件构建成功；隔离目录 6 项模型发现测试全部通过，真实从自有镜像下载新版 RIFE 包后，解压目录、安装标记和 5 个 NCNN 模型发现均正常。
- 最新 EXE、DLL 和布局已部署到 `C:\Program portable\3FUI\plugin`，安装版仍显示 v1.10.1。下一步重启 3FUI 检查下载分类和模型下拉框，再在 NVIDIA 机器实测 GIMM-VFI、GMFSS 与 TensorRT。
- 当前源码和 HandShake 记录尚未提交；本地分支仍为 `ahead 9, behind 4`，建议实际界面确认后提交，生成的 EXE/DLL 不纳入 Git。

### 2026-08-23 20:58

- 项目正式采用独立版本 `1.11.0`，上游版本只作为同步基线单独记录为 `1.4.2`，以后自动更新只比较独立 SemVer。
- 已加入基于 ModelScope 的插件更新：启动后台检查和底部手动检查按钮；发现新版后必须确认，下载会校验文件大小和 SHA-256，不会静默覆盖。
- 更新器只允许替换 EXE、插件 DLL 和布局 JSON；等待 3FUI 退出后先备份再替换，失败自动回滚，成功自动重启，并在下次启动显示结果。
- 已新增可重复使用的 Release 生成脚本和更新器测试脚本。隔离测试覆盖正常更新、路径穿越包、篡改包和文件锁回滚，四项全部通过。
- 已创建公开 ModelScope 数据集 `AerithDream/VideoEnhancer-Releases` 并发布 1.11.0。加强回滚测试后重新发布的远端 ZIP 为 15,143,282 字节，SHA-256 与 `stable.json` 一致；编译后的插件读取远端后正确判断当前已是最新版。
- 1.11.0 三文件已部署到 `C:\Program portable\3FUI\plugin`，源/目标哈希一致。下一步重启 3FUI 检查底部按钮和“已是最新版”提示，再确认退出/自动重启的实际体验。
- Git 仍为 `main...origin/main [ahead 9, behind 5]`，工作树不干净且未提交。建议完成实机界面确认后提交本批源码与记录，再单独处理远端分叉。

### 2026-08-23 21:15

- 已发布独立版本 1.11.1，上游同步基线保持 1.4.2。
- 模型下载页右上方新增“下载插件更新”，与底部“检查更新”使用完全相同的稳定清单、版本比较、用户确认、大小/SHA-256 校验、备份回滚和自动重启流程；插件包不会混入模型列表或解压到模型目录。
- 为了真实测试，本机先部署了带新按钮的过渡 1.11.0 DLL，然后才发布远端 1.11.1。目前安装版仍是 1.11.0，源码和远端是 1.11.1，这是刻意保留的升级条件。
- 1.11.1 ZIP 已上传公开 `AerithDream/VideoEnhancer-Releases`，大小 15,143,363 字节，SHA-256 为 `1ED57927DF9B81315A27BB79097B10576C25E5239914EB7EAF01B6477448AE62`；1.11.0 历史包仍保留。
- 已安装的 1.11.0 插件实际读取到远端 1.11.1，判断有更新并成功下载，文件长度和 SHA-256 与清单一致。更新器四项隔离回归也全部通过。
- 启动 3FUI 后若先出现自动更新提示，请先点“否”，再进入“模型下载”点击“下载插件更新”，即可专门验证兜底入口。更新完成应自动退出并重启到 1.11.1。
- 已为本次测试启动本机 3FUI；窗口保持运行，等待上述操作。
- Git 仍为 `main...origin/main [ahead 9, behind 5]`，工作树不干净且未提交；完成这次实机测试后建议立即提交，再单独处理远端分叉。

### 2026-08-23 23:59

- 已发布独立版本 1.11.3（上一轮 21:27 曾发布过 1.11.2，精简更新弹窗文案，当时未记录，已补入版本历史）。
- 版本管理全面整顿：CLI 内部版本号不再手写字面量，运行时直接读工程文件的版本（单一来源），新增 `--version` 一键查询；发布脚本版本默认自动取自插件源码，构建后还会实际运行 EXE 校验版本一致，杜绝多处版本号漂移。
- 版本检查改为以 GitHub 为唯一标准：插件向 `maxzrb/VideoEnhancer` 的最新 Release 读取清单，并核对标签与清单版本一致。更新包下载首选 ModelScope 镜像，失败自动回退 GitHub，两路都校验大小和 SHA-256。
- 发布流程一键化：`release\build-modelscope-release.ps1 -PublishGithub -PublishModelScope` 自动构建、打包、创建 GitHub Release 并同步 ModelScope（不传开关则只构建并打印手动命令）。
- 按你的反馈调整界面：底部“检查更新”和模型页“下载插件更新”都移到各自栏的最右侧；更新确认弹窗不再显示“更新内容”段，只留版本、大小和确认提示。
- 实机验证全部通过：过渡 1.11.2 插件真实从 GitHub 发现 1.11.3；ModelScope 读取延迟期间自动走 GitHub 兜底完成下载并校验；真实执行更新替换后，安装目录三个文件与发布包逐字节一致，EXE 报告 1.11.3。期间还意外验证了一次“更新器无法替换自身时自动回滚”的保护逻辑。
- 已知事项：当晚 ModelScope 读取缓存延迟超过一小时未追平（服务端已确认写入），期间旧版客户端看不到 1.11.3，新版不受影响；你下次打开 3FUI 应看到“已更新到 v1.11.3”，顺便看下更新按钮的新位置（新弹窗样式要等 1.11.4 才会再出现）。
- Git：1.11.3 相关三个提交已推送到 fork `maxzrb/VideoEnhancer` 并打 tag `v1.11.3`；主线相对上游 `ahead 12 / behind 5` 保持独立维护不合并不变。

### 2026-08-24 00:15

- 应你的要求，独立版本编号自 1.0 系重新开始，已发布 1.0.3；功能内容与 1.11.3 完全相同，仅重置编号（旧 1.9.x–1.11.x 是从接手时的 1.9.5 一路叠上来的过渡编号）。
- GitHub Release `v1.0.3` 已上线并确认为 latest；ModelScope 已同步写入（其读取缓存仍在延迟，服务端数据已确认最新）。
- 本机 3FUI 已手动升级到 1.0.3，三文件与发布包哈希一致，下次启动会显示"已更新到 v1.0.3"。
- 注意：已装 1.11.x 的机器（如 3060 机）不会自动提示 1.0.3（版本号数值更小），需手动覆盖三个文件一次；之后 1.0.4 起恢复自动更新。
- Git：版本切换与记录已提交并推送 fork；主线相对上游保持独立不合并不推送。

### 2026-08-24 00:25

- 你发现的完全正确：此前 ModelScope"读取延迟"其实是发布脚本漏了 `--repo_type dataset`，更新包全部误传进了自动创建的同名**模型**仓库，数据集从未收到新版本；本地断点缓存不分仓库类型，把后续重试也骗过去了。
- 已修复：发布脚本补上参数、清除断点缓存后重新上传，数据集现已正确提供 1.0.3 的清单和 ZIP（全部 7 个文件确认提交）。
- 需要你手动处理一件事：误建的模型仓库 `AerithDream/VideoEnhancer-Releases` 只能在网页上删除（API 被禁），打开 https://www.modelscope.cn/models/AerithDream/VideoEnhancer-Releases 在设置里删除即可。


## 2026-08-24 00:59

- 已重新通读 1.0.3 当前主线，确认版本号来源、GitHub/ModelScope 更新链路、插件队列 Hook、CLI 管线、RVE 包装器和本机便携后端结构；本轮保持 1.0.3，不发布、不部署。
- TensorRT 补帧现只选择 RIFE PyTorch 权重，由 RVE 按实际阶段分辨率自动构建 Engine。先补后超使用源分辨率；先超后补在同后端时使用放大后分辨率，跨后端时由中间视频真实尺寸决定。GIMM-VFI、GMFSS 仍只支持 CUDA/PyTorch。
- 插件与 CLI 构建通过，顺序管线测试 6/6 通过；隔离模型测试确认 TensorRT 不会误选 GIMM、GMFSS 或 `.engine`。在线模型列表只展示 `Backend/python_20260823.7z`。
- “两个 Python”中，实际运行的是 `python\python\python.exe`；另一个位于 `Lib\venv\scripts\nt`，只是 Python 标准库的 venv 模板。真正占空间的重复项是本地镜像缓存的两个 Python 压缩包，合计约 5.27 GB。
- 当前缺口：ModelScope 的 RIFE 包只有 NCNN 模型，没有 RIFE `.pth/.pt/.pkl`，所以界面暂时没有可选的 TensorRT 补帧权重；还需在 NVIDIA 机器做首次构建和缓存命中实测。建议先提交本轮源码与记录，再处理权重镜像和 1.0.4。

## 2026-08-24 01:28

- 已先提交 RIFE TensorRT 补帧接入，提交为 `25dba3b`；版本仍为 1.0.3，没有发布或部署。
- 已从 RVE 官方模型 Release 补齐 5 个 RIFE 权重：4.6、4.7、4.25、4.26、4.26 heavy。文件逐个上传到公开的 `AerithDream/VideoEnhancer-Models/Frame-Interpolation/RIFE/`，没有改私有、没有同步删除其他文件。
- 5 个远端文件的路径、大小和 SHA-256 均与本地官方资产一致；真实用 CLI 下载 4.6 权重也通过 SHA-256。TensorRT 补帧下拉现能发现全部 5 个权重。
- 上传后顺带发现 ModelScope tree API 默认只返回前 100 条，旧 CLI 会漏掉后面的模型。现已改为分页读取，模型下载页从截断的 85 项恢复为完整 105 项，旧 RIFE、Param-Bin、PTH 和预置 TensorRT Engine 都能继续显示。
- CLI 构建、单文件发布和 6 项顺序管线测试全部通过。剩余工作是在 NVIDIA 机器实测仅补帧、先超后补、先补后超的首次 Engine 构建与第二次缓存命中，再决定是否发布 1.0.4。

## 2026-08-24 12:27

- 已确认 3FUI 未运行，并将当前工作区的 `videoenhancer.exe`、`videoenhancer.3fui.dll` 和 `videoenhancer-layout.json` 替换到 `C:\Program portable\3FUI\plugin`。
- 三项安装文件均已与源文件 SHA-256 校验一致。现在可以启动 3FUI 测试完整 ModelScope 模型列表以及 RIFE TensorRT 权重显示。

## 2026-08-24 12:39

- 实机发现补帧开关灰色不可点，原因是当前选择了 `BasicVSR++（视频）`；该后端目前明确不支持和 RIFE 补帧组合，并非控件损坏。
- 已把 5 个 RIFE TensorRT 权重下载到实际 3FUI 的 `models\Frame-Interpolation\RIFE`，并清除配置里的错误 `InterpModel: "[]"`。
- 安装版 CLI 已验证能列出全部 5 个 TensorRT RIFE 权重。测试时请先把超分后端切换为 `TensorRT (NVIDIA)`，再打开补帧开关并选择 RIFE 模型；BasicVSR++ 仍不能组合补帧。

## 2026-08-24 12:52

- 修复补帧模型下拉框重复：新 `Frame-Interpolation/RIFE` 和旧 `models\RIFE` 同时存在时，NCNN 只保留新路径，旧路径仅在新模型缺失时兼容显示。
- 已替换安装目录中的新版 `videoenhancer.exe`，实际 CLI 验证 NCNN 只显示 5 个 RIFE 模型，TensorRT 显示 5 个 RIFE 权重。
- 请在 3FUI 中刷新模型列表；若仍是旧缓存，重启 3FUI 即可。

## 2026-08-24 12:57

- 确认远端重复资源来自两个 RIFE 归档：`Frame-Interpolation/RIFE.7z` 和旧兼容路径 `RIFE/RIFE.7z`。两者内部都是同一套五个 NCNN 模型，大小和压缩结果不同但内容等价；五个 RIFE `.pkl` 是独立的 TensorRT/CUDA 权重，不属于重复项。
- CLI 现在只在新客户端下载列表隐藏 `RIFE/RIFE.7z`，不会删除或修改 ModelScope 远端文件，旧客户端仍可使用旧路径；新版归档、5 个 `.pkl` 和其他补帧资源继续显示。
- 重新构建并替换 `C:\Program portable\3FUI\plugin\videoenhancer.exe`，安装版实测模型清单由 105 项变为 104 项，RIFE 区域已不再出现重复归档。版本保持 1.0.3。

## 2026-08-24 13:06

- 修复运动补帧开关在切换后端后仍灰色的问题：页面之前如果加载过 `BasicVSR++`，切换到 `TensorRT`、`CUDA` 或 `NCNN` 后，旧代码没有重新启用开关。
- 新插件 DLL 已构建并替换到 `C:\Program portable\3FUI\plugin`，哈希校验一致。重启 3FUI 后重新选择 TensorRT + NCNN + RIFE，补帧开关应可点击；BasicVSR++ 仍保持禁用，这是该后端不支持组合补帧的预期行为。
- 修复已提交：`1e2c054 fix: re-enable interpolation after backend switch`。

## 2026-08-24 13:19

- 核对 ModelScope 原始远端树后，明确可清理的旧版兼容资源：`Backend/python.7z`（与 `python_20260823.7z` 重复，约 2.46 GiB）和 `RIFE/RIFE.7z`（与新版 `Frame-Interpolation/RIFE.7z` 内容重复，约 48 MiB）。用户已决定不兼容旧版，因此这两项可以删除，分别保留日期版 Python 和新版 RIFE 归档。
- `TensorRT-Default` 的 15 个预置 Engine 约 188.5 MiB，当前仍受 CLI 支持；只有决定完全强制首次自动构建时才建议删除。PTH、ONNX、FlashVSR、BasicVSR++、NCNN 包和五个 RIFE `.pkl` 不属于旧版兼容资源，暂不建议清理。
- 本轮只核对，没有执行远端删除，等待确认具体清理范围。

## 2026-08-24 13:36

- 核对结果：超分 TensorRT 的 PTH 路径已有设备缓存复用和失效重建，缓存键包含源模型摘要、GPU、TensorRT 版本、输入尺寸和分块尺寸，并用互斥锁避免并发重复构建。
- 发现缺口：CLI 虽然把输入尺寸写进缓存名，但没有把宽高/分块参数传给转换器，转换器仍默认静态 `1920x1080`；外层缓存键也没有 Torch-TensorRT 版本。有效但 profile 不覆盖当前输入的 Engine 可能直到实际推理才失败。
- RIFE TensorRT 使用 RVE 内部缓存，参数和设备变化会生成不同名字，但损坏/不兼容 Engine 目前只是加载失败，不会自动删除并重建；同名权重被替换也不会触发缓存失效。远端预置 `*-x2-tensorrt.engine` 失效时通常无法自动匹配对应 PTH。
- 本轮只做源码核对，没有改实现，也没有 NVIDIA 实机验证。当前结论是：超分缓存“基本具备”，RIFE 和预置 Engine 的自动重建仍需补强。

## 2026-08-24 15:10

- TensorRT 超分任务现在只接受 PTH 源模型，启动任务时按当前 GPU、输入宽高、输出倍率、分块和精度自动生成或复用 `models\TensorRT-Cache`，不再直接使用上游预置 Engine。
- 缓存键新增 Torch-TensorRT 版本、精度、优化级别、转换配置、缓存 schema、tile padding 和源权重 SHA-256；已有 Engine 会验证当前请求是否落在 profile 内，失效后自动删除并重建。
- RIFE TensorRT 缓存加入权重 SHA-256。flow/encode 任一 Engine 加载失败会删除成对缓存并重建；构建完成后反序列化失败也会清理并重试。构建阶段输出事件，3FUI 任务进度会显示阶段和百分比。
- 模型转换页面保持原有调用兼容，实时显示同一套 TensorRT 构建进度，并从输出中提取最终 Engine 路径；转换页输出仍写入 `models\TensorRT-Personalized`，与任务缓存分离。
- CLI、插件和 Python 静态验证通过：CLI/插件构建成功，RVE 顺序后端测试 6/6，相关 Python 文件 `py_compile` 通过；本机无 NVIDIA，未执行真实 Engine 编译。
- 已将修正版 Python 文件写入并上传公开 ModelScope `Backend\python_20260824.7z`（大小 3,447,393,513 字节，SHA-256 `dc399b4dc257b64b09d3175ac9afa3ca66bc388bc40e6313c9b85c5559055b17`）；版本保持 1.0.3，远端预置 Engine 暂未删除。建议提交当前源码和记录。

## 2026-08-24 15:35

- 修复启动环境检测误导：插件启动先显示“正在检查运行环境…”，模型目录尚在读取/准备时显示非错误的加载中状态；只有 ffmpeg、Python、后端脚本或 Python 库等基础组件缺失才显示“环境检测未通过”。环境检查超时也不会把等待状态误报成失败。
- 修复补帧开关状态分裂：切换到 BasicVSR++ 时会同步关闭配置、开关和状态标签；切回 TensorRT、CUDA、NCNN 等可组合后端时保持关闭状态，开关显示与“关闭”文本一致。后续手动开启补帧后仍按配置正常保存。
- 插件构建通过，最新 DLL 已替换到 `C:\Program portable\3FUI\plugin`，源目标 SHA-256 均为 `82603a6f6c0797058440a5695f7c97cf9426b32d13ce3a400ca3663e93c1fe3a`。版本保持 1.0.3，尚未进行 NVIDIA 实测。

## 2026-08-24 16:05

- 针对截图中“补帧状态文字为关闭、开关外观仍为蓝色开启”的问题，统一三个视频超分页面的补帧开关初始化和刷新路径。
- LakeUI 自绘开关现在在同步配置后显式执行 `Invalidate`、`Refresh`、`Update`，并带有控件释放保护；BasicVSR++ 切换后禁用且显示关闭，切回 TensorRT/CUDA/NCNN 后保持真实关闭状态并恢复可点击。
- 已重新构建并部署插件 DLL：源目录和 `C:\Program portable\3FUI\plugin` 目标文件 SHA-256 均为 `E478BFEA006D8ADB7949829FDAD6827F5C1C419B39EFB9C9C02631D2ED6E755D`。版本保持 1.0.3。
- 当前机器没有 NVIDIA，也没有完整 UI 自动化环境；请重启 3FUI 后验证 BasicVSR++ ↔ TensorRT 切换及手动开关操作。建议实测通过后提交当前源码和记录。

## 2026-08-24 16:20

- 用户截图确认普通 WinForms 刷新仍不足以更新 LakeUI 自绘开关：滑块仍停在开启位置，但状态文字为关闭。
- 新增 LakeUI V3 渲染队列刷新调用，兼容没有该私有 API 的旧宿主版本，并重新构建 DLL。
- 新 DLL SHA-256：`7A94ABF6E8A20495B0E5F49B9F1FB5E1EC5F1D8AD5C640D6CFA59B550A250D75`。部署时发现 3FUI 正在占用旧 DLL，未强制结束进程；请退出 3FUI 后再覆盖安装测试。

## 2026-08-24 16:25

- 用户退出 3FUI 后，已成功覆盖部署新插件 DLL。
- 工作区与安装目录文件 SHA-256 均为 `7A94ABF6E8A20495B0E5F49B9F1FB5E1EC5F1D8AD5C640D6CFA59B550A250D75`。
- 现在可以启动 3FUI 验证 BasicVSR++ 下关闭/禁用、切回 TensorRT 后关闭/可点击，以及手动开启时滑块和状态文字同步变化。

## 2026-08-24 16:45

- 查明开关矛盾的真正原因：LakeUI 开关有 300ms 动画，切换到 BasicVSR++ 后立即禁用会停止动画但保留右侧的旧进度，因此字段是关闭、滑块仍像开启。
- 修复同步顺序：临时将动画时长设为 0，先让 Checked 立即落到目标位置，再设置 Enabled，最后恢复动画时长。
- 新插件 DLL 已重新构建并部署，源/目标 SHA-256 均为 `811BF85019877A31F90EF0EBF678065902B1E14AE73CAEEC3B6EE3748DF570A9`。请重启 3FUI 后重新测试。

## 2026-08-24 17:00

- 根据截图修复主页面底部“检查更新”按钮白框贴住最底部边框的问题：状态行高度由 48px 增至 60px，并增加 8px 底部内边距。
- 新 DLL 已构建，工作区 SHA-256 为 `71F5439F5F071823A5BF5D23E651B3AF891C710FD95C2A3A251162AD1024C959`。
- 部署时 3FUI 正在运行并锁定安装目录 DLL，未强制结束进程；退出宿主后再覆盖部署。

## 2026-08-24 17:05

- 用户退出 3FUI 后已成功覆盖部署底部间距修复版插件。
- 源 DLL 与安装目录 DLL SHA-256 均为 `71F5439F5F071823A5BF5D23E651B3AF891C710FD95C2A3A251162AD1024C959`。
- 请启动 3FUI 检查“检查更新”按钮和窗口底部边框之间的间距。

## 2026-08-24 17:30

- 修复环境检测复发的误报：TensorRT 预置 Engine 不兼容、模型库/补帧库为空属于可选资源，不再被插件当成基础环境失败；只有 ffmpeg、Python、后端脚本和 Python 库缺失才显示红色错误。
- 模型下载页的“下载插件更新”改为“下载全部”，批量下载资源列表中所有未安装文件。
- ModelScope 已新增 `Plugin/videoenhancer.exe`，资源列表会显示插件 EXE，下载目标为插件程序目录。
- CLI 已重新构建并部署，安装版 EXE SHA-256 为 `62D1A92FDF206017991170CFBC9B1E26DFA2C07041726D6A2DB1A09D4491C74F`。插件 DLL 因 3FUI 正在运行暂未覆盖，退出宿主后继续部署。

## 2026-08-24 18:05

- 修复模型下载页“下载全部”重复取得 `videoenhancer.exe`：资源列表仍显示 `Plugin/videoenhancer.exe`，但批量下载会排除当前插件 EXE；单项下载和自动更新流程不受影响。
- 自动更新源顺序统一为 GitHub 首选、ModelScope 兜底：检查版本时 GitHub API/Release 清单不可达会读取 ModelScope `stable.json`；下载更新包时 GitHub 资产失败会回退 ModelScope 镜像，均校验大小和 SHA-256。
- 版本提升至 1.0.5，已发布 GitHub Release `v1.0.5` 和 ModelScope Releases `1.0.5`。两边清单版本一致，远端 URL 返回 200。
- 验证：CLI/插件构建通过；更新器成功、路径穿越、篡改、回滚隔离测试 4/4 通过；本机无 NVIDIA，真实 TRT 未验证。
- 当前 Git 工作树有本轮和此前累计未提交修改，建议先提交后再进行宿主实测或切换工具。

## 2026-08-24 18:20

- 新增 `release/发布流程.md`，作为后续版本发布前的门禁和操作指导。
- 文档区分本体发布与模型资源发布：本体使用 GitHub Release + ModelScope Releases 双源，模型单独进入 ModelScope 模型仓库；明确排除 `PotPlayer.7z`。
- 流程包含版本源一致性、构建、ZIP/逐文件 SHA-256、更新器成功/路径穿越/篡改/回滚测试、GitHub→ModelScope 发布顺序、模型清单验收、双源交叉核验、失败重试/补丁版本和发布记录收尾。

## 2026-08-24 18:40

- 重写根目录 `README.md`，改为中性的功能说明和使用文档，覆盖安装、后端、模型下载、CLI、自动更新、故障排查、构建和许可证边界。
- 删除原作者教程、外部网盘链接、比较性宣传和个人评价；README 不再使用“独立维护版”等表述，只显示当前版本 `1.0.5`。

## 2026-08-24 18:55

- 当前 `1.0.5` 功能、README、发布流程和项目记录已提交为 `8e59154 release: 1.0.5`，并推送到 GitHub `maxzrb/VideoEnhancer` 的 `main` 分支。
- 发布流程现默认要求发布完成后提交并推送 `fork/main`，同时核对远端提交；不会把原作者仓库 `origin` 作为发布推送目标。

## 2026-08-24 19:20

- 版本升至 1.0.6，本体发布和自动更新改为单 EXE：插件 DLL 内嵌在 EXE 中，布局 JSON 内嵌在 DLL 中，不再分别作为 Release 资产。
- 自动更新时，新 EXE 作为临时更新器等待 3FUI 退出，替换目标 EXE并释放 DLL；EXE 或 DLL 任一步替换失败会恢复原文件。
- 发布脚本改为生成 `VideoEnhancer-<version>-win-x64.exe` 和 `stable.json`，并自动同步 GitHub、ModelScope Releases 以及模型页 `Plugin/videoenhancer.exe`。
- 隔离测试通过：正常更新、篡改 EXE 拒绝、无效资产拒绝、DLL 锁定回滚。按用户决定不兼容旧 ZIP 协议，1.0.5 及更早版本需要手动安装一次 1.0.6。

## 2026-08-24 19:35

- 1.0.7 已发布到 GitHub Release、ModelScope Releases 和模型仓库 `Plugin/videoenhancer.exe`，三处 EXE 大小均为 17,417,235 字节，SHA-256 均为 `877b64f1920eac60732b0aa959eaba5a22779da59ab0566735fbedddf1751cef`。
- GitHub 和 ModelScope 的 `stable.json` 均指向 1.0.7 单 EXE；GitHub Release 只有 EXE 与 `stable.json` 两个资产。
- 实际 3FUI 插件目录保留 1.0.6 EXE/DLL，便于直接测试 1.0.6→1.0.7：检查更新、下载、退出 3FUI、替换 EXE、从新 EXE 释放 DLL并自动重启。
- 1.0.7 更新器隔离测试通过正常更新、篡改拒绝、无效资产拒绝和 DLL 锁定回滚；本机无 NVIDIA，未验证真实 TensorRT。

## 2026-08-24 19:45

- 1.0.7 源码已提交并推送到 `maxzrb/VideoEnhancer`，提交为 `12a0bf2 release: 1.0.7 exe-only updater`。
- 推送前检测到 GitHub 上有新的 README 编辑，已保留原作者链接、ModelScope 可点击链接和 README 精简内容，再将 1.0.7 变基后推送；未强推，也未触碰原作者仓库。
- 本地提交与 `fork/main` 已核对一致；实际 3FUI 仍保持 1.0.6，可直接开始 1.0.6→1.0.7 自动更新测试。

## 2026-08-24 19:50

- 已从作者 ModelScope 完整下载并校验 `Backend/python_20260824.7z`，大小 2,672,713,470 字节，SHA-256 为 `ebe7c07a41f3b7d62127d327eed57e76580d617086ea50b89255d88ff09729cf`；只在 `D:\read` 隔离审计，没有覆盖当前 3FUI。
- 作者确实加入了 FlashVSR/BasicVSR++ 低内存修复：视频改为有限时间窗口解码、推理后立即写入 FFmpeg，不再把整段输入和输出留在内存。作者自带的长/短视频窗口测试 2/2 通过，相关 Python 文件编译通过。
- 当前五个 RIFE `.pkl` 都能被后端正确识别。实际障碍不是 `.pkl` 本身，而是“模型转换”页面和通用超分转换器只接受 `.pth`；该转换器面向单帧 NCHW 超分，不能正确转换 RIFE 这种多输入时序模型，补帧必须走独立的 RIFE flow/encode Engine 构建流程。
- 不建议用作者 Python 包整包覆盖，因为会丢失我们后来增加的 TensorRT 缓存键、失效重建、构建进度和 profile 验证。下一步应选择性合并低内存文件，并按权重内部架构重做 CUDA/TRT 补帧模型筛选和预构建入口。

## 2026-08-24 20:30

- 已选择性合并作者 FlashVSR/BasicVSR++ 的低内存实现：有限时间窗口解码与推理、结果立即写入 FFmpeg，不再保存整段输入输出；长/短窗口测试 3/3 通过。
- CUDA/TensorRT 补帧现在读取权重内部结构，不再按扩展名猜测。`.pth/.pt/.pkl` 都可检查；CUDA 支持 RIFE、GMFSS、GIMM，TensorRT 当前只显示和接受 RIFE。本机 5 个 RIFE `.pkl` 全部识别并进入 TensorRT 列表。
- 新增 RIFE 专用 TensorRT 预构建流程，直接构建 RVE 的 flow/encode Engine，不再调用单帧超分转换器。无 CUDA 环境会明确提示，避免原生崩溃。
- 模型转换页已支持 `.pth/.pt/.pkl` 并自动分流：普通超分走原转换器，RIFE 走专用预构建；GMFSS/GIMM 提示改用 CUDA。插件与 CLI 均已真实构建通过。
- 用户决定先将源码提交远端，再换 3060 设备验证 CUDA、TensorRT 和长视频内存；全部验证无误前不发布 1.0.8 Release。

## 2026-08-24 21:00

- 已从自己的 ModelScope 仓库重新下载下午版后端作为正确基线，原包大小 3,447,393,513 字节、SHA-256 为 `dc399b4d...9055b17`；没有采用作者 2.67 GB 包作为发布基线。
- 在下午版后端上叠加 8 个低内存/RIFE 辅助文件，逐文件哈希 8/8 一致，并确认下午版 TensorRT 转换器、Engine 验证器和 RIFE TRT 核心文件未改变。新包通过 7-Zip 整包测试。
- 新后端已覆盖上传到 `AerithDream/VideoEnhancer-Models/Backend/python_20260824.7z`。远端回读大小为 3,447,405,317 字节，SHA-256 为 `3e55dcc3e773ceb5098d3b6bd90e9d490462b6323a5931d0aaba9e983a61482b`。
- 本次只更新后端模型资源并提交开发源码；不发布 1.0.8，不修改 stable.json。换到 3060 设备后先完成 CUDA/TRT 和长视频内存实测。

## 2026-08-24 21:15

- 本轮功能源码已提交为 `753a3a4` 并推送到 `maxzrb/VideoEnhancer` 的 `main`，本地与远端提交一致；没有推送原作者仓库。
- 换 3060 设备后拉取 `fork/main`，并下载 ModelScope revision `c0d5b6c09e08df7a2b25af49ed6f73e586a4bd1c` 的后端包继续测试。
- 当前版本保持 1.0.7；本地 CUDA/TRT 与长视频内存验证通过前不发布 1.0.8。

## 2026-08-24 21:30

- 修复 `RIFE/rife4.26.heavy` 被误报“补帧模型名不唯一”。问题不是模型重复，而是旧解析器把模型名中的 `.heavy` 当成扩展名，错误地又匹配了普通 `rife4.26`。
- 现在完整相对架构路径优先精确匹配；短名称只剥离 `.pth/.pt/.pkl/.engine` 这些真实权重扩展名，并统一规范路径分隔符。真正存在重名时会列出可选路径。
- 使用当前 3FUI 模型目录完成 6 组回归：heavy 的相对路径、短名、带 `.pkl` 路径均识别为 `RIFE425_heavy`；普通 4.26 仍识别为 `RIFE425`。CUDA 列表 8 项、TensorRT 列表 5 项均正常。
- 修复版 EXE 已覆盖到 3FUI 插件目录，SHA-256 为 `B18D735C4B213CAE00B851E7238D39F58C19D086189425E9544E0CB59CB8BE62`。可以直接重试补帧任务，不需要重启 3FUI。
- 版本仍为 1.0.7，未发布 1.0.8；解析修复和记录尚未提交。终端未允许删除回归测试 EXE 副本，但配置不会加载它。

## 2026-08-24 21:45

- 确认 `.pkl` 不是本次崩溃原因：补帧权重已成功加载并识别为 `RIFE425_heavy`；真正问题是 CUDA 主后端把 NCNN 格式的 EfficientNet 转场模型目录交给了 `torch.jit.load`。仅把扩展名换成 `.pth` 不会解决问题。
- CLI 现在按主后端选择转场检测器：CUDA/TensorRT 使用 RVE 内置且无需外部模型的 `pyscenedetect`，NCNN/ONNX 继续使用现有 EfficientNet NCNN 检测；转场检测阈值仍然生效。
- 已在 RTX 3060 上实际运行 `rife4.26.heavy.pkl` CUDA 补帧：任务退出码 0，640x360 输入 24 帧、输出 47 帧，符合 2 倍补帧 `2N-1`。
- 修复版 EXE 已部署到 3FUI 插件目录，SHA-256 为 `64268C901BB124697A04509DE6999D469C8FD7987706C2655D19A6AD7910FE16`；未更改 DLL、Python 后端或模型文件。
- 版本保持 1.0.7，未发布 1.0.8。下一步重试原始 1920x1080 完整任务，再验证 TensorRT 首次构建/缓存和时序模型长视频内存。

## 2026-08-24 22:00

- 修复 GMFSS Fortuna Base 读取失败。Base 权重本来就不包含 Union 使用的 `rife` 组件，旧后端却强制按 Union 加载；现在会读取权重自带的 `model_type`，Base 使用专用 `FusionNet_b`，Union 继续使用 `FusionNet_u` 和 IFNet。
- 优化补帧模型下拉框：GMFSS/GIMM 通过内存映射快速读取轻量元数据，并按模型路径、大小和修改时间缓存架构结果。当前 12 个 CUDA 权重首次扫描约 3.98 秒，缓存后连续读取约 0.37–0.50 秒；更换模型文件会自动重新检查。
- RTX 3060 上 Base、Union 模型初始化均通过；GMFSS Base 实际补帧任务输入 24 帧、输出 47 帧，退出码 0。正式 EXE 已部署，SHA-256 为 `9F2959857B5D67E2B837213E80E70B5933173263728B74D725FDFF0E4EF1B819`。
- 当前 GMFSS 在本项目中仅支持 CUDA/PyTorch。NCNN 与 TensorRT 下拉框均不会显示 GMFSS；当前 RVE 的 GMFSS TensorRT 尚未实现。更正：外部 Enhancr / vs-gmfss 已有 GMFSS Union 与 Fortuna TensorRT 实现，但不是本项目当前可直接启用的 RVE 原生路径。
- 版本保持 1.0.7，未发布 1.0.8。下一步重试原始 1920x1080 GMFSS Base 任务，并按实际素材比较 Union/AnimeRun。

## 2026-08-24 22:10

- 用户决定暂不接入 GMFSS TensorRT。此前“GMFSS Fortuna 没有 TensorRT 实现”的判断已更正：Enhancr 及 vs-gmfss 已实现 Union/Fortuna TRT，只有当前使用的 RVE 原生 GMFSS 路径尚未实现。
- 对本机 RVE 2.4.1-dev17 做了模型能力审计。补帧工厂支持 RIFE、GIMM、GMFSS、IFRNet；前三者已接入，尚缺的是 IFRNet PyTorch/CUDA。IFRNet 的 TensorRT 仅回退到 PyTorch，NCNN 补帧仍只支持 RIFE。RIFE 4.15、4.18、4.20、4.22/4.22-lite 等旧权重尚未进入当前 ModelScope 镜像，但底层兼容代码已有。
- RVE 还支持可叠加的 1x 修复模型链，官方模型包括 DeH264、DRUNet、DnCNN；本机已有部分 NCNN/PTH/ONNX 1x 权重，但插件没有独立的解压缩、去噪和多模型顺序入口。
- 建议后续优先加入 IFRNet CUDA，再做 DeH264/DRUNet/DnCNN 修复链。RIFE DRBA 当前被命令行硬编码关闭、ONNX 补帧主路径未完整接线，暂不列为可直接接入模型。
- 本次只更新审计与交接记录，没有修改功能代码、模型和部署文件；版本保持 1.0.7，未发布 1.0.8。

## 2026-08-24 22:20

- 核对了去压缩/去噪与后续不同后端的兼容性。RVE 的 1x 修复模型没有独立后端参数，会跟当前进程共用 CUDA、TensorRT 或 NCNN，并固定在补帧和超分之前执行。
- 修复与后续第一项操作使用相同后端时可以在一个进程中逐帧完成；后端不同时需要扩展现有管线，通过 RGB FFV1 无损中间视频拆成 2–3 个阶段。当前项目的阶段规划只覆盖超分与补帧，尚未包含独立修复步骤。
- HDR 修复只能选择 CUDA/PyTorch 或经过验证的 TensorRT。NCNN 和 ONNX 阶段会失去 RVE 的 HDR 帧管线，后续再切换 CUDA 也无法恢复。
- DeH264 最适合作为首批兼容测试。标准 DnCNN 可测试 NCNN/CUDA，但 PyTorch 通常需要 FP32。DRUNet 当前包装路径会丢失自动拼接噪声图的适配函数，不能直接标记为可用；TensorRT 也必须逐模型编译验证，不能因为 RVE 提供入口就全部放行。
- 多个修复、补帧和超分模型在同一进程内会同时占用显存，RTX 3060 6 GB 有 OOM 风险；跨进程更稳，但会增加临时文件空间与时间。本次未修改功能代码、模型或部署文件，版本保持 1.0.7。

## 2026-08-24 23:25

- 已建立代表模型 GPU 全流程矩阵：4 帧短视频、逐项输出尺寸/帧数校验、失败日志、断点续跑及 CSV/Markdown 报告。剔除已验证不支持的 TensorRT 架构后，计划共 578 项：单模型 50、同后端组合 150、跨后端组合 378。
- 单模型 50/50 已全部通过。实机覆盖 NCNN、CUDA、TensorRT、ONNX、FlashVSR、BasicVSR++ 的代表超分，以及 RIFE、GIMM、GMFSS Base/Union 代表补帧。
- 测试过程中已修复 NCNN 完整成品后的 Vulkan 清理误报、GIMM 新权重字段/FP32/最低尺寸、CUDA SwinIR/GRL 精度、渲染线程异常挂死、ONNX 名称与倍率解析、TensorRT 低分辨率探测、缓存路径过长和 GRL FP32 Engine。
- AnimeSR、SwinIR、CRAFT 已实机确认不能进入当前 TensorRT 单图直接 Engine 路径，现已从 TensorRT 下拉列表排除，避免用户首次运行才失败；GRL 使用 FP32 Engine 后通过。
- 正式 EXE 已同步，版本仍为 1.0.7，未发布 1.0.8。下一步运行同后端 150 项和跨后端 378 项，并对失败项修复复测后交付完整通过性表格。

## 2026-08-25 00:35

- RTX 3060 代表模型全流程矩阵已完成。有效计划 578 项全部取得终态：576 项通过、2 项因显存不足跳过、0 项功能失败或超时。分项为单模型 50/50、同后端组合 150/150、跨后端组合 376 通过及 2 跳过。
- 测试按用户要求先使用 3 路并行；检测到 FlashVSR + GIMM 的 CUDA OOM 后降为 2 路。2 路下 FlashVSR 仍因与另一时序模型共享 RTX 3060 6GB 显存而 OOM，用户决定不再测试并记录原因。两种流程均保留完整日志，最终状态为 `SKIP_OOM`，不计作功能失败。
- 修复 BasicVSR++ 被旧入口判断错误禁止与补帧组合的问题。跨后端本来会使用 RGB FFV1 无损中间视频分成两个独立进程，移除误拦截后 BasicVSR++ 与 NCNN/CUDA/TensorRT RIFE、GIMM、GMFSS Base/Union 共 12 项双顺序组合全部通过。
- 同后端组合测试中还修复了 TensorRT GRL FP32 超分输出交给 RIFE FP16 时的张量 dtype 不一致；顺序包装器现在在阶段边界对齐插帧器 dtype，修复后相关组合通过。
- `gpu_matrix_runner.py` 支持断点恢复、3 路并发、高显存 GIMM 错峰、OOM 终态分类及通过/跳过/失败分栏报告。GIMM 在 160x120 会产生 NaN，256x192 已验证稳定；时序超分 + GIMM 使用 256x192，其余 GIMM 保留 320x240。
- 最终报告位于 `test-results/gpu-matrix/summary.md`、`matrix.md` 与 `matrix.csv`。Release 构建 0 错误，Python 语法检查通过，顺序包装器单元测试 6/6 通过，矩阵零待测项复核退出码 0。
- 修复版正式 EXE 已覆盖到 `C:\Program portable\3FUI\3FUI\Plugin\videoenhancer.exe`，与 `cli/publish/videoenhancer.exe` 的 SHA-256 一致。版本保持 1.0.7，未发布 1.0.8；当前工作树尚未提交，建议用户确认报告后提交并推送 `fork/main`。

## 2026-08-25 09:18

- 用户决定暂停 1.0.8 发布，先解决 Python 后端无法可靠更新的问题。本轮没有修改版本号、创建 Release/标签或上传资产。
- 现有 Backend 下载属于直接覆盖解压：压缩包解到正在使用的 `python` 目录，同名文件会覆盖，但新版已删除、改名或移动的旧脚本不会清除；解压中断也没有完整回滚，可能留下新旧混合环境。
- 插件当前只要检测到 `python\python\python.exe` 就把最新后端包显示为“本地已安装”，无法判断安装版本或远端 SHA-256，因此已有后端的用户通常不会获得更新入口。
- 当前 EXE 会在启动时针对已知 RVE 2.4 结构修补 GMFSS、GIMM、ONNX 和补帧检查脚本，可暂时兼容本轮改动，但它不是完整后端更新机制，未来结构变化后可能无法继续补丁。
- 建议 1.0.8 发布前先实现事务式后端更新：版本/哈希标记、活动目录外下载、同盘暂存解压、关键文件与 Python 健康检查、退出任务后目录交换、失败回滚和旧版备份。新环境确认正常后再清理备份，不直接删除未知的用户自定义文件。

## 2026-08-25 09:27

- 后端更新将优先考虑类似游戏启动器的文件级增量包，而不是每次要求用户重新下载约 3.4GB 全量 Python。当前远端基线后只修改了 4 个脚本，原始总量约 47KB，增量下载预计可控制在几十 KB。
- 增量包会记录基础版本、目标版本、新增/替换/删除文件以及更新前后 SHA-256。客户端只有在本地基础版本和旧文件哈希匹配时才应用补丁，避免把补丁强行套在未知或被用户修改的环境上。
- 更新过程采用事务日志：补丁下载到活动目录外；受影响旧文件先备份；删除操作先移动而非永久删除；新文件验证后原子替换；健康检查通过才写入新版本标记。中断或验证失败可以自动回滚。
- 没有版本标记的老用户可通过已知关键文件哈希识别公开基线并补写标记。无法识别、环境损坏或补丁链过大时才使用全量修复包；全量 `python_YYYYMMDD.7z` 仍保留用于新安装和最终兜底。
- 不建议直接对压缩后的 7z 文件制作二进制差分；重新压缩会导致大面积字节变化，并且强依赖完全一致的旧归档。第一版先做稳定、可审计的文件级补丁，未来大型 DLL 频繁变化时再评估内容分块。

## 2026-08-25 10:09

- 后端增量更新机制已经完成实现。客户端现在会识别本地后端版本，并按下载量选择最小补丁链；补丁只携带新增或变化的文件，同时可明确删除已经废弃的旧脚本，不再依赖直接覆盖 3GB 以上完整包。
- 更新前会核对旧文件 SHA-256，用户自行修改过的文件不会被静默覆盖。变更文件先备份，写入过程使用事务日志和原子替换；健康检查失败会立即回滚，进程意外中断后下次启动也会先恢复。
- 没有版本标记的老用户可通过稳定哨兵文件识别已发布基线；旧 CLI 已经自修补过部分文件时，新补丁可幂等跳过相同内容并补写版本标记。无法识别或损坏的环境会改走完整修复包，在暂存目录检查 Python 后再整体切换，旧脚本不会残留。
- 插件下载页已把 Backend 从普通模型下载中独立出来，显示“当前版本 → 最新版本”和本次实际下载大小。Backend 不进入三路并行、“下载本组”或“下载全部”，避免更新期间同时运行多个后端任务。
- 已新增补丁生成器、通道清单示例和自动测试。隔离测试 6/6 通过，覆盖增量成功、SHA 冲突拒绝、健康检查回滚、中断恢复、完整修复和部分自修补兼容；CLI 与插件构建、既有 6 项顺序包装器测试、Python 语法检查均通过。
- 1.0.8 仍保持暂停，本轮没有上传后端补丁或 `Backend/channel.json`，也没有创建 Release/标签。下一步需要用真实公开后端基线生成首个生产补丁并在实际 3FUI 安装目录验证，再决定恢复发布。

## 2026-08-25 10:34

- 发布流程已经加入强制后端审计。以后每次发布都必须提供上一已发布后端目录和本次候选目录，由脚本逐文件比较大小与 SHA-256，不能凭人工判断跳过。
- 后端没有变化时版本必须保持不变；有变化时必须递增后端版本，并提供完整包和稳定哨兵。脚本会验证完整包与候选目录完全一致，再自动生成新增、替换、删除清单、增量包、`channel.json` 和审计报告。
- 后端有变化时，正式发布固定先上传完整包，再上传增量包，最后上传 `Backend/channel.json`；远端回读内容、大小和哈希一致后，才允许创建 GitHub Release，避免新版 EXE 先发布而后端通道尚未就绪。
- GitHub Release 正文现在只接受 `[更改]xxxx`、`[新增]xxxx`、`[移除]xxxx` 三种行首格式。每个更新条目必须单独一行，不允许空行、自由段落或一行多个事项；`stable.json` 与 GitHub 正文使用同一份 UTF-8 Notes 文件。
- 已增加 Release Notes 示例和发布门禁自动测试。4 项测试全部通过：合法模板、非法自由文本拒绝、Backend 新增/替换/删除自动制包、完整包与候选目录不一致时拒绝。
- 版本仍为 1.0.7，1.0.8 发布继续暂停；本轮没有上传文件、创建 Release 或标签。

## 2026-08-25 10:56

- 1.0.8 本体已完成发布前构建和隔离验证，当前尚未上传。EXE 版本为 1.0.8，大小 17,471,662 字节，SHA-256 为 `ee510e5599029e6637fdd072f8b64a78dacab07b21d3a6f28c54a8c401afd46e`。
- 已从当前公开后端包提取旧基线并与候选目录逐文件核对。排除 Triton 运行缓存后，真实变化为 7 个脚本替换、无新增和删除；本地增量包仅 14,699 字节。
- 按用户要求，本次只准备发布插件本体，Backend 完整包、增量包和 `Backend/channel.json` 均不上传、不激活。发布脚本新增显式暂缓模式，仍保留真实审计，避免跳过后端变动检查。
- `UpstreamBase/upstreamBase` 已从版本常量、更新清单对象、稳定清单生成和当前发布流程中移除；生成的 1.0.8 `stable.json` 已确认不再包含该字段。
- CLI/插件构建、顺序包装器 6/6、后端事务测试 6/6、发布门禁 5/5、Python 语法检查和 EXE 更新器四类隔离测试全部通过。下一步提交并推送源码，再发布 GitHub 与 ModelScope 本体并回读核对。

## 2026-08-25 11:04

- 1.0.8 已正式发布。源码提交 `2ed6c2e` 和标签 `v1.0.8` 已推送到 `maxzrb/VideoEnhancer`，GitHub Release 地址为 `https://github.com/maxzrb/VideoEnhancer/releases/tag/v1.0.8`。
- GitHub Release 正文与规定的逐行 `[更改]`、`[新增]`、`[移除]` 模板完全一致，资产只有 `VideoEnhancer-1.0.8-win-x64.exe` 和 `stable.json`。
- GitHub、ModelScope Releases、模型页 `Plugin/videoenhancer.exe` 三处文件均为 17,471,664 字节，SHA-256 均为 `fd249941331cbaa139cb52d770b1e60ec7d2454c7c65eb00fb9495a78345d820`。GitHub 与 ModelScope 稳定清单版本、路径、大小和哈希一致，均不再包含 `upstreamBase`。
- 本次未上传 Backend 完整包、增量包或 `Backend/channel.json`，后端更新通道没有激活。用户可先测试 1.0.7→1.0.8 本体自动升级；真实 Backend 升级需等后端资产另行发布。

## 2026-08-25 11:18

- 已定位 1.0.7→1.0.8 自动更新关闭 3FUI 后不重启的原因。本机结果文件显示旧 `videoenhancer.exe` 在替换瞬间仍被占用，更新失败；旧更新器又只在成功后执行重启，因此把 3FUI 留在关闭状态。当前正式安装仍是 1.0.7，没有发生半更新。
- 更新器现在会在 3FUI 退出后对 EXE/DLL 的短暂共享冲突重试 10 秒；无论替换成功还是失败，只要确认旧宿主已经退出，都会重新启动 3FUI并在下次启动显示结果。宿主本身未退出时不会重复启动第二实例。
- 自动测试覆盖正常更新、2 秒短暂占用后成功、篡改拒绝、无效包拒绝、永久占用回滚，以及真实等待宿主 PID 后失败仍重启，共 6 类场景，全部通过。CLI 和插件构建也已通过。
- 远端 1.0.8 没有被覆盖或修改。该修复需要按版本规则发布为 1.0.9；目前等待确认后再递增版本并发布。

## 2026-08-25 11:38

- 用户已确认发布 1.0.9 和相应后端更新。版本、更新说明和本体产物已经准备完成，GitHub 当前还没有 v1.0.9，尚未开始上传。
- Backend 2026.08.25.1 完整包已生成，大小 2,790,829,396 字节，SHA-256 为 `8c598d90e594a4e3957b48421f780c491cd06c73fb1914600b69950b0a44ab2d`。新包明确排除了旧 3.4 GB 归档、Triton 缓存、Python 缓存和本地版本标记。
- 完整包两次解压后与候选目录逐文件一致；增量审计结果为替换 7 个后端脚本、无新增和删除。生产增量包已在真实旧文件副本上执行成功，更新后 7 个文件哈希一致、Python 健康检查通过、版本标记为 2026.08.25.1，channel 状态为最新。
- 1.0.9 自更新、短暂占用重试、篡改拒绝、无效包拒绝、永久占用回滚、失败后重启共 6 类测试通过；后端事务 6/6、发布门禁 5/5、顺序管线 6/6、CLI/插件构建和 Python 语法检查也全部通过。
- 修复了发布门禁测试覆盖正式 Backend 审计文件的问题。测试现在使用独立临时输出目录，并已验证不会再改变正式制包结果。下一步提交推送源码，再按完整包、增量包、channel、本体的顺序发布。

## 2026-08-25 11:58

- 1.0.9 已正式发布，源码提交为 `e6f5344`，标签为 `v1.0.9`，GitHub Release：`https://github.com/maxzrb/VideoEnhancer/releases/tag/v1.0.9`。
- Backend 2026.08.25.1 已按完整包、增量包、channel 的安全顺序上传并激活。完整包大小 2,790,829,396 字节，SHA-256 为 `8c598d90e594a4e3957b48421f780c491cd06c73fb1914600b69950b0a44ab2d`；从 2026.08.24.1 升级的补丁只有 14,698 字节，SHA-256 为 `57ac66c15f88f8de90044b26f40a9ab9462018ed0d79efbd332f186f870ccf60`。
- GitHub、ModelScope Releases 和模型页三处 1.0.9 EXE 均为 17,471,986 字节，SHA-256 均为 `ceee62ae201e57efde39955f219d95e565899f8c307955495088c2553a682ddb`。两处稳定清单内容一致，Release 正文符合逐行分类模板。
- 使用正式 1.0.9 客户端读取真实安装目录和公开 Backend channel，正确识别为 2026.08.24.1→2026.08.25.1、增量模式、下载 14,698 字节，没有误选 2.79 GB 全量包。
- 本机正式 3FUI 仍保留 1.0.7，便于真实测试：手动重新打开 3FUI，更新到 1.0.9并确认自动重启，再到模型下载页执行 Backend 增量更新。

## 2026-08-25 12:27

- 已查明 1.0.9 Backend 增量更新被拒绝的原因：3FUI 启动后自动运行的环境自检正在加载 3840x2160 TensorRT Engine，占用了 `videoenhancer.exe` 和 Python 后端；14,698 字节增量包本身没有损坏。
- 启动自检现已轻量化，只检查 FFmpeg、Python、必要脚本、依赖导入、模型目录和后端版本，不再逐个反序列化 TensorRT Engine。完整 Engine 诊断仍可通过显式校验命令执行，实际使用模型时也会按需检查。
- 插件现在会跟踪自己的启动自检，并在插件本体更新、Backend 更新、停用或关闭 3FUI 前取消整个自检进程树。真正的视频处理任务仍会阻止 Backend 热更新，安全门禁没有降低。
- CLI 和插件均构建通过，Backend 更新事务测试 6/6。真实 TensorRT 安装环境中的新自检约 1.27 秒完成，退出码 0，未再加载任何 Engine。本轮尚未修改版本号、提交或发布，建议作为 1.0.10 修复发布。

## 2026-08-25 13:12

- 1.0.10 已正式发布：源码提交 `a7220fb`、`main` 和标签 `v1.0.10` 一致，GitHub Release 为 `https://github.com/maxzrb/VideoEnhancer/releases/tag/v1.0.10`。
- GitHub、ModelScope Releases 和模型页三处 EXE 均为 17,473,022 字节，SHA-256 均为 `8f890047b20344fac530a98f8bc01adfc8c80624787b57a67da77dd5618ce37c`；两端稳定清单也已回读一致。
- 发布前从公开 Backend 2026.08.25.1 完整包重新解压并逐文件审计，结果没有新增、替换或删除，因此本次没有重新上传 2.79 GB Backend 包，也没有制作新补丁；用户无需重复下载后端。
- 从 1.0.9 首次升级时，如果旧版 TensorRT 启动自检仍在运行，请先等待该进程结束再点本体更新。进入 1.0.10 后，插件会在本体或 Backend 更新前自动停止自己的后台自检。

## 2026-08-25 13:47

- 已确认另一台电脑的 Backend 增量更新因 `GMFSS.py` 旧文件 SHA-256 不匹配而安全停止。无法仅凭当前电脑的文件判断另一台电脑为何不同；但客户端不应让用户只能反复点击同一个失败的增量更新。
- 新版候选在增量补丁冲突或健康检查失败并回滚后，会把 Backend 行改成“增量补丁不适用”，并提供“下载完整修复包”操作，同时显示完整包大小。
- 完整包约 2.79 GB，不会自动下载。用户点击后还会看到确认框；确认后客户端明确跳过补丁，下载并校验完整包，再以事务方式替换受管理后端，失败仍可回滚。
- CLI、插件、单文件构建和 Backend 隔离测试均已通过。测试覆盖了“通道本来存在可用补丁，但用户强制选择完整包”的路径。本轮尚未提交或发布，建议作为 1.0.11 发布；Backend 资源版本无需变化。

## 2026-08-25 14:09

- 1.0.11 已正式发布，源码提交为 `d859f7b`，标签为 `v1.0.11`，GitHub Release：`https://github.com/maxzrb/VideoEnhancer/releases/tag/v1.0.11`。
- GitHub、ModelScope Releases 和模型页三处 EXE 均为 17,473,820 字节，SHA-256 均为 `cdfb7f38688778e332da15566e88a98680ebc662624d49d52e0b1f19fbb4027f`；两端 `stable.json` 也已回读一致。
- Backend 2026.08.25.1 审计没有文件变化，因此本次没有重复上传约 2.79 GB 的完整包，也没有制作新补丁或修改 channel。
- “下载完整修复包”采用事务式整体替换：新包先校验并暂存，旧后端整目录移入备份后再切换；成功才清理旧备份，失败或中断会自动恢复。因此旧脚本会被清除，同时保留失败回滚能力。
- 另一台电脑更新到 1.0.11 后，如果增量补丁仍因旧文件 SHA-256 不匹配而停止，Backend 行会切换为“下载完整修复包”；只有用户确认后才下载约 2.79 GB 完整包。

## 2026-08-25 17:28

- 1.1.0 发布候选已完成。全新安装会固定生成 `Plugin\videoenhancer\videoenhancer.exe`，插件 DLL 仍放在 `Plugin` 根目录；旧版的 EXE、模型、Backend、FFmpeg 和运行状态会通过可恢复事务迁入子目录。
- 已在真实 3FUI 安装上完成迁移，47,676 个受管文件、约 24.99GB 内容迁移前后 SHA-256 完全一致；插件页正确显示新 EXE 路径，环境检测和更新后的自动重启均通过。
- GPU 矩阵扩展到实际安装的全部 129 个超分模型、22 个补帧模型及代表组合，共 679 项。五路并行结果为 677 项通过、2 项 FlashVSR+GIMM 因 6GB 显存不足跳过、0 项功能失败。
- 测试发现并修复了两个倍率识别问题：ONNX 文件名中的 `1x3xHxW` 不再被当成倍率，TensorRT 缓存名中的输入尺寸也不会干扰原模型倍率；相关模型和 `fix1/fix2` 文件名防回归均已用最终候选实测通过。
- Backend 2026.08.25.1 的干净基线审计无变化，本次不会重复上传约 2.79GB 完整包，也不会制造空增量补丁。安装器、自动更新器、Backend 事务、顺序管线和发布门禁均已通过，下一步提交并正式发布 v1.1.0。

## 2026-08-25 18:34

- 用户考虑到 1.1.0 刚发布、影响面较小，明确授权不升版本号覆盖本次修复。模型下载页 Backend 行的状态改为“已是最新 / 无需操作”，真实 3FUI 截图确认文字已与同一行垂直居中，不再上浮。
- 根 README 已按模型家族覆盖当前 129 个超分条目和 22 个补帧条目，补充原作者、原项目或可追溯来源链接，并明确格式转换不改变原权利归属；暂时无法确认原始发布页的条目如实标为待核实。
- 覆盖时 `main` 与 `v1.1.0` 均指向功能提交 `46d7a02`；发布记录随后单独追加到 `main`。GitHub、ModelScope Releases 和 ModelScope Models 三处 EXE 回读均为 17,549,975 字节，SHA-256 为 `6420df26aac81dd10285e5e79c7d561928ec15d9a259c7a67f97d842d943d3c0`；GitHub 与 ModelScope 稳定清单均为 962 字节且哈希一致。
- Backend 2026.08.25.1 与公开干净基线审计仍为 0 新增、0 替换、0 删除，因此没有制作或上传后端完整包、补丁或 channel。

## 2026-08-25 20:16

- 新增内置模型能力清单，覆盖当前 93 个唯一模型和 129 个后端组合。已知模型的倍率、架构、支持后端和固有输入倍数不再依赖文件名猜测；用户自行加入的未知模型仍保留兼容回退。
- 对全部现有 ONNX 与 PTH 超分模型调查了奇怪分辨率兼容性。4 个 RealHatGAN ONNX 需要宽高为 16 的倍数，AnimeSR 需要 4 的倍数，3 个 2x RRDB/ESRGAN 权重需要 2 的倍数；后端现会自动补边，完成推理后裁回用户原始尺寸。
- RealHatGAN 的 35x33、50x47 CUDA 回归已通过。1920x1080 在本机 6GB 显卡上使用较大分块会显存不足，实验性 128 分块可正确输出 3840x2160，但该数值只作为测试记录；按要求没有写进能力清单，也没有作为默认值，默认仍跟随后端自动设置。
- 选择补帧倍率后不再弹出“手动指定帧率”的提示窗，倍率仍会正常保存并传给后端。
- CLI 构建、单文件裁剪发布、能力清单测试 3/3、Python 语法与差异检查均通过。插件源码已修改，但本机先前的 3FUI 宿主引用程序集临时目录已被清理，暂时无法重新编译插件 DLL。本轮尚未提交、推送或发布，版本仍为 1.1.0。

## 2026-08-26 01:57

- 1.1.1 发布候选已经完成：新增模型导入、能力修正、内置能力清单和 LakeUI 二级模型菜单，修复尺寸敏感模型及 ONNX 分块参数链路，移除对比工具和独立模型指南。
- Backend 升级为 2026.08.26.1，相对旧版为 1 个新增、2 个替换；约 3.07 GB 完整包和约 12 KB 增量包均已制作并逐文件复验。
- CLI、插件和单文件构建通过；16 项模型/协议测试、安装器、自动更新器、Backend 事务 6/6、顺序管线 6/6、发布门禁 5/5 全部通过，正在提交和上传正式资产。

## 2026-08-26 12:00

- 1.1.1 与 Backend 2026.08.26.1 已正式发布。GitHub Release、ModelScope Releases、模型页备用 EXE 以及 Backend 完整包、增量包和 channel 均已上传并回读核验。
- 发布后检查发现模型下拉框虽然使用 LakeUI 二级菜单，但 `ModernComboBox` 的内部文本编辑能力没有关闭。现已在统一下拉框配置中设为只允许选择，模型、后端、倍率、分块等选项不再能手工输入。
- 热修复沿用 1.1.1 覆盖发布：新 EXE 为 16,873,792 字节，SHA-256 为 `fd9adfcc8428a6a3309d52ed866b8ed3bb5ccba6754adb58e164b5b0d6bd51af`；GitHub 与 ModelScope 回读一致，安装器 5 类回归通过。
- 本机 3FUI 当前正在运行，插件 DLL 处于占用状态；已安排退出宿主后自动替换，重新启动 3FUI 即可看到不可编辑的模型下拉框。

## 2026-08-26 12:34

- 用户实机截图证明首轮下拉框修复无效。反编译已安装 DLL 后查明，同一配置函数后面还保留着旧的 `Editable=True`，把前面新增的 `False` 覆盖了；此前只检查源码新增行和文件哈希，没有检查最终赋值顺序。
- 现已将最后一次赋值改为 `Editable=False`，并重新编译、再反编译成品核对。最终插件与本机安装 DLL 都只有一条 `combo.Editable = false`，不存在后续重新开启编辑的代码。
- 1.1.1 三处资产已再次覆盖。最终 EXE 大小 16,873,786 字节，SHA-256 为 `bed5db6baa5d318d3e54fb8b8fa1a95165ef5114df5c481ce9e083594f63b1b7`；本机 3FUI 当时已退出，DLL 和 EXE 已直接更新完成。

## 2026-08-26 13:10

- 模型架构菜单出现在屏幕顶端不是坐标转换错误，而是 LakeUI 发现完整菜单放不下后向上翻转，随后又被工作区边界钳制到顶端。现按当前屏幕、DPI、模型框位置和架构数量动态缩小根菜单行高，优先从模型框下方展开。
- 更新流程恢复两阶段确认：发现版本时只确认是否下载；下载并校验完成后，再弹出默认选择“否”的“确认重启安装”。用户拒绝时不会停止任何检查、不会启动更新器，也不会关闭 3FUI。
- 插件和 1.1.1 EXE 已重新构建，成品反编译确认菜单适配与二次确认的执行顺序正确。新 EXE 为 16,874,426 字节，SHA-256 为 `bda65213b198c57da7e926ea29cc66963c73f2a09e5c2e48236ebe7b2727589a`；线上三处资产已覆盖，本机将在当前 3FUI 正常退出后自动替换。

## 2026-08-26 17:54

- 视频超分和运动补帧各新增一个“半精度推理”开关，默认开启为 FP16 优先、不兼容时自动回退；关闭则只对该阶段强制 FP32。两个阶段精度完全独立，同后端的先超后补、先补后超仍在一个进程内完成，不会为了精度不同再生成 FFV1 中间视频。
- 半精度文字最初使用了错误的小号说明字体且预留空间不足，现改为 11pt 正文样式、按 DPI 和实际文字宽度布局并垂直居中；本机高 DPI 截图已确认两行显示完整、间距正常。
- RTX 3060 已实测单超、单补，以及两种同后端组合的混合精度，尺寸、帧数和退出码均正确；TensorRT 精度会进入 Engine 缓存键，FP16 与 FP32 不会误复用。
- 全新 Windows 10 依赖审计完成：本体 EXE 自包含，不要求另装 .NET；便携 Python 和原生推理扩展需要 Microsoft VC++ 2015–2022 x64 运行库；CUDA 13.0 后端建议 NVIDIA 580 或更高驱动；NCNN 只需要显卡驱动提供 Vulkan，不需要 Vulkan SDK。环境检查现会按所选后端实际导入 NCNN、PyTorch、TensorRT 或 ONNX 并检查 GPU，但不会加载大型模型或 Engine。
- 最终插件/CLI 构建、六类后端依赖检查、安装器五场景、Backend 事务 6/6、发布门禁 5/5、有序管线 8/8 均通过。本轮按要求只创建本地提交，不推送、不发布。

## 2026-08-26 19:02

- 已发布 1.1.2。新增超分和补帧独立半精度开关；同后端组合保持单进程并分别使用精度参数；环境检查补充 Windows 10、VC++、CUDA/TensorRT、ONNX 和 NCNN 依赖诊断。
- 发布前严格审计 Backend 2026.08.26.1，结果为 0 新增、0 替换、0 删除，因此没有重复上传完整后端包、增量包或 channel。
- GitHub Release `v1.1.2`、ModelScope Releases 和模型页备用 EXE 均已同步。EXE 大小 16,878,865 bytes，SHA-256：`da4bbd96d2f21792c1b85abb1c185e2366e766b7131d8e8f2cc2379abb754605`；stable.json 与两端回读一致。
- 正式发布脚本完成插件/CLI 构建、安装器五场景、更新器七场景、Backend 事务 6/6、发布门禁 5/5 和有序管线 8/8。源码提交 `a5047c5` 已推送到 `fork/main`，当前工作树保持干净。
## 2026-08-25 19:46

- 已同步独立维护主线到 `fork/main` 最新提交 `26c795c`。首次默认拉取误指向原作者 `origin/main` 并产生冲突，已完整中止，没有把原作者分支混入当前主线；本地 `main` 的默认上游现已修正为 `fork/main`。
- OpenModelDB 官方当前共有 671 个模型。按实际 RVE 2.4、内置 Spandrel、CLI 扫描规则、RGB 通道和 ONNX 输入契约核对后，不能宣称全部支持：架构层面覆盖 633 个，当前可进入运行路径的综合静态上限约为 620 个，且其中 19 个 ONNX-only 模型仍需逐个实跑。
- 已确认 51 个明确缺口：31 个没有当前架构或格式路径，11 个是灰度/Alpha 模型而 RVE 视频管线固定 RGB，6 个只有 safetensors 可形成新增路径但界面不扫描，3 个 TSCUNet 是时序 ONNX、不能进入当前单图 ONNX 后端。
- 详细架构表、模型名单和修复顺序已写入 `docs/openmodeldb-rve-support-audit.md`。本轮只做审计和记录，没有修改运行代码、版本号或发布资产；建议下一步先补 safetensors/ckpt 发现和模型元数据预检，再验证 19 个 ONNX-only 候选。

## 2026-08-27 15:28

- 已将本地 `main` 从 `26c795c` 快进同步到独立维护线 `fork/main` 的 `30bf203`，包含 1.1.2 发布记录和半精度控制等远端更新；未合并原作者 `origin`。
- 同步前暂存并恢复了本地 OpenModelDB 审计文件与记录更新，未丢失本地内容；当前运行代码与 `fork/main` 一致。
- 当前仍有 `docs/openmodeldb-rve-support-audit.md` 和两份 HandShake 记录待提交，建议审核后提交并推送；本轮未运行构建或业务测试。

## 2026-08-27 15:54

- 按反馈优化模型选择菜单：一级架构分类关闭无用的左侧图标/勾选预留空白，二级具体模型仍保留勾选区域。
- 为内置模型项增加 LakeUI 风格悬浮提示，提示包含模型用途简介、放大倍率和支持后端；RIFE、GMFSS、GIMM 等补帧模型也有对应说明。
- 当前菜单使用自定义 `ModernContextMenu`，LakeUI 3.22.0 没有公开菜单项 Tooltip 属性，因此通过 `FloatingToolTipForm` 绑定实际悬停项；菜单关闭和插件销毁会释放提示。
- 插件构建和成品 DLL 运行时探针已通过：一级 `IconSize=0`，二级 `IconSize=24` 且勾选尺寸保留；代表性 AniSD 提示文本完整，提示控制器可正常构造/关闭。真实 3FUI 中的鼠标位置和观感仍需确认。

## 2026-08-27 16:05

- 已将最新构建的 `videoenhancer.3fui.dll` 放入本机 3FUI 实际插件目录 `C:\Program portable\3FUI\plugin`。
- 部署前确认 3FUI 主进程未运行；部署后文件大小为 4,635,136 bytes，SHA-256 为 `B49BA31904E911EA04B2A5829B31528DB07D14416BF2AD6A5C5E69CDA2F4C365`，与仓库构建产物一致。
- 版本号和发布资产未变化；仍需启动 3FUI 做一次真实菜单与悬浮提示交互确认。

## 2026-08-27 16:35

- 模型悬浮说明已改为按具体模型和变体编写：区分题材、倍率、去噪强度、轻重版本、输入约束、后端限制和试用建议，不再使用笼统的 Real-ESRGAN 系列描述。
- 所有 13 个插件选项框已禁止关闭状态下被鼠标滚轮误切换，覆盖推理后端、补帧后端、补帧倍率、转场阈值等；打开下拉列表后仍可在列表内滚动。
- 内置“使用教程”已重写为面向小白的逐步手册，详细说明程序路径、模型下载/导入、后端和模型匹配、参数选择、组合处理、试片与排错。
- 插件已重新构建并覆盖到 `C:\Program portable\3FUI\plugin\videoenhancer.3fui.dll`；文件大小 4,658,176 bytes，SHA-256 为 `5FD9B0B2BF85C7B02BB5EA7C03C927707CF5EC9DD34E6B5DEB19735FF9BEAA0C`。仍需启动 3FUI 做真实鼠标交互确认。

## 2026-08-27 16:57

- 按反馈将内置页标题固定为“使用教程”，删除模型简介和教程中的短片、试片、几秒素材及 A/B 对比等措辞，保留具体的模型适用条件、参数含义和操作步骤。
- 插件已重新构建并覆盖到 `C:\Program portable\3FUI\plugin\videoenhancer.3fui.dll`；文件大小 4,657,664 bytes，SHA-256 为 `FBA27AC1EAA05B4D86BAE3D197B7F7B482AFEE26AFF820DAFF8EA4FCB765441D`。文案探针和 `git diff --check` 已通过。

## 2026-08-27 17:42

- “模型导入”页新增用户模型删除：选中模型后按 `Delete`，或在模型行上单击右键选择“删除用户模型”；两种入口都会先弹出确认框。
- CLI 新增 `--delete-user-model <ID>`，删除时校验路径只位于 `models\User` 的模型容器内，并同步移除模型文件/目录和 `model-catalog.json` 记录；写清单或清理失败会回滚。
- 删除成功后，插件会重新加载用户模型列表，并刷新超分、补帧工作台目录；导入页说明和插件 README 已补充操作方式。
- 验证：CLI 与插件构建通过，全量 Python 测试 18/18；隔离目录删除实测通过；安装版帮助已出现删除参数。新的 DLL 与 CLI 已部署到 `C:\Program portable\3FUI\plugin`，源/目标哈希一致。当前版本仍为 1.1.2，未产生版本迭代记录。

## 2026-08-27 18:15

- 已正式发布 1.1.3。提交 `5a7d309` 已推送到 `fork/main` 和 `fork/release/1.1.3`，远端标签 `v1.1.3` 已创建。
- GitHub Release：<https://github.com/maxzrb/VideoEnhancer/releases/tag/v1.1.3>；包含 `VideoEnhancer-1.1.3-win-x64.exe` 和 `stable.json` 两个资产，Release 状态为正式、非草稿、非预发布。
- ModelScope Releases：<https://www.modelscope.cn/datasets/AerithDream/VideoEnhancer-Releases>；Models 备用 EXE：<https://www.modelscope.cn/datasets/AerithDream/VideoEnhancer-Models>。两处 EXE 大小均为 16,877,609 bytes，SHA-256：`16e1728a99a2ed568909bdc44df1c7df7cea7356a4135e7661e57a127859bf50`；stable.json 为 673 bytes，SHA-256：`0da8e3cb5e49a62b199c048a3ecac41f36d38db539c9cfdffa29c25ad3060276`。
- Backend 2026.08.26.1 发布前逐文件审计为 0 变化，因此未重复上传后端完整包、增量包或 channel。官方发布脚本完成 CLI/插件构建、全量 Python 测试 18/18、发布门禁 5/5、安装器五场景和更新器隔离测试，全部通过。
- 当前仍未在真实 3FUI 宿主中用鼠标逐项确认悬浮提示和下拉框滚轮体验，源码与构建产物验证已完成。

## 2026-09-01 08:40

- 继续完成 LakeUI 5.0 全插件原生控件迁移：主面板六页、模型能力编辑器和四宫格窗口已统一使用 LakeUI 控件；新增 `ModernGridPanel`/`ModernHorizontalPanel` 只做布局计算，不自行绘制。
- 已删除旧 `Panel`、`TableLayoutPanel`、`FlowLayoutPanel`、`WebBrowser`、原生标签/图片框/数值框/复选框/下拉框及 `FluentCardPanel`、`FluentProgressBar`、`SmoothButton`、旧自绘时间轴；实时预览改为 `PixelPictureBox`，进度条改为 `ExcellentProgressBar`，教程改为 `MarkDownViewer`，四宫格时间轴直接使用 `ExcellentTrackBar`。
- `build.ps1` 新增 LakeUI 主版本门禁，官方 LakeUI `5.0.0.0` 基线构建成功，旧 `3.22.0.0` 构建按预期拒绝；成品引用确认是 `LakeUI, Version=5.0.0.0`。
- 验证结果：迁移源码和整个插件静态扫描无旧可视控件/自绘视觉类命中，CLI Python 测试 18/18，`git diff --check` 通过。尚未在真实 3FUI 中执行 DPI 截图、500 帧预览压力和四宫格鼠标回归；当前源码及记录未提交，建议实机回归前先提交。

## 2026-09-01 08:51

- 额外用官方 3FUI 6.2.2 x64 宿主程序集（LakeUI 5.1.0.0）完成兼容构建检查，并重新用 LakeUI 5.0.0.0 生成最终构建产物；旧 3.22 宿主仍被门禁拒绝。
- 教程页已接入 `MarkDownViewer.LinkClicked`，点击链接会交给系统默认浏览器；四宫格视频槽使用 `ModernPanel.Image` 的 Zoom 模式保持缩略图比例，关闭时先解除图片引用再释放位图。

## 2026-09-01 08:57

- 最终再次以 LakeUI `5.0.0.0` 构建并核对插件引用；旧 LakeUI `3.22.0.0` 构建按预期被版本门禁拒绝，静态旧控件扫描和 `git diff --check` 通过。
- 既有 CLI Python 测试 `18/18` 通过。本轮未修改 CLI、视频处理、模型、配置或队列业务逻辑；源码、构建门禁和记录仍待提交。

## 2026-09-01 09:03

- 在加载 LakeUI 5.0 与宿主程序集后完成类型元数据门禁：`PluginPanel`、`QuadGridForm`、视频槽和布局容器均未包含原生 WinForms 可视控件字段；允许的宿主外壳和 LakeUI `ModernPanel` 类型符合预期。
- 真实 3FUI 的截图、DPI、500 帧压力和鼠标交互仍待宿主执行，当前不将这些项目宣称为已通过。

## 2026-09-01 09:07

- 修正静态扫描命令的正则转义后，迁移源码及整个 `VideoEnhancerPlugin` 均通过旧 WinForms 可视控件/自绘视觉类门禁；`git diff --check` 无错误，仅有换行格式提示。
- 工作树仍未提交；建议在真实 3FUI 宿主回归前先创建本地 Git 提交。

## 2026-09-01 09:14

- 按用户要求将 LakeUI 构建与运行时基线从 5.0 升级为官方 5.1；构建脚本、运行时提示和 README 依赖说明均严格限制 `5.1.x`。
- 使用官方 3FUI 6.2.2 宿主与 LakeUI `5.1.0.0` 构建通过，产物引用已核对；LakeUI 5.0 和旧 3.22 均被门禁拒绝。真实 3FUI 截图、DPI、500 帧和鼠标回归仍待执行，源码及记录尚未提交。

## 2026-09-01 09:16

- 最终以 LakeUI `5.1.0.0` / 3FUI 6.2.2 基线构建成功；CLI Python 测试 `18/18`、全插件旧控件静态门禁和 `git diff --check` 均通过。
- 当前工作树仍未提交或推送；真实 3FUI 的六页截图、DPI、500 帧预览压力和四宫格鼠标回归仍需宿主环境执行。

## 2026-09-01 11:35

- 实际 3FUI 已自动更新为 6.2.3，随附 LakeUI 5.3.0.0；构建与运行时策略调整为 LakeUI 5.1 最低版本、允许后续 5.x，不再限制为严格 5.1.x。插件成品引用已核对为 FFmpegFreeUI 6.2.3.0 / LakeUI 5.3.0.0。
- 修复 LakeUI 5.3 `ModernPanel` 提前布局导致的派生控件空引用，并移除 `ModernTabControl.BackgroundSource` 全树绑定，解决宿主启动后 D3D 渲染递归无响应。
- 修复组合框文本横向滚动偏移：新增 `LakeComboBox` 在选项/字体/尺寸变化时重置内部偏移；补帧后端列由 29% 调整为 34%，组合框内边距收紧。实机截图确认上下两处 `TensorRT (NVIDIA)` 完整显示，模型名从 `AnimeJaNai...` 开始显示。
- 覆盖安装到 `C:\Program portable\3FUI\3FUI\Plugin\videoenhancer.3fui.dll`，SHA-256 为 `793D869D27682E233E9E532BB6BC013C4AF4154C309874235E16D70D53A781C1`；启动后插件可用，六个标签页逐页切换保持响应。CLI Python 测试 18/18、静态控件门禁和构建均通过。
- 仍待 100%/125%/150% DPI 截图矩阵、500 帧预览压力、四宫格完整交互和运行时控件树递归检查；本轮源码和记录未提交。

## 2026-09-01 11:48

- 最终重建并覆盖安装实际 3FUI 插件，DLL SHA-256 为 `BB3E64617A6D4445184ABB5265AF03AAA623D8BF61569A78F6FC75CC7CF6F049`。实际 3FUI 6.2.3 / LakeUI 5.3.0.0 启动正常。
- 依次点击六个插件标签页（实时预览、模型下载、模型转换、模型导入、使用教程、超分工作台），宿主始终响应；组合框截图确认两个 `TensorRT (NVIDIA)` 完整显示，模型名从 `AnimeJaNai...` 开头显示，不再出现只显示右半段的问题。
- 本轮仍未完成 100%/125%/150% DPI 矩阵、500 帧预览压力、四宫格完整鼠标流程和控件树递归检查；源码和记录尚未提交。

## 2026-09-01 11:52

- 清理新增文本的混合换行并保留已有文件的 UTF-8/BOM 与历史换行布局，重新构建并覆盖安装实际插件；最终 DLL SHA-256 为 `695E1ADA472755B13AC60404FA579A852CF401C88948E7D4BBE3E65CDC5A8B50`。
- 实际 3FUI PID 29648 再次启动并打开插件，六个标签页逐页点击均保持响应；最终截图确认上下两个 `TensorRT (NVIDIA)` 完整显示、模型名从 `AnimeJaNai...` 开始显示。
- `git diff --check` 退出码为 0（仅有 core.autocrlf 换行转换提示），CLI Python 测试 18/18、静态旧控件门禁和 LakeUI 5.3 构建均通过；尚未完成 DPI/500 帧/四宫格完整回归，源码和记录未提交。

## 2026-09-01 11:58

- 收尾核对再次通过：旧 WinForms 可视控件静态扫描 PASS、CLI Python 测试 18/18、`git diff --check` 退出码 0；实际 3FUI PID 29648 保持响应。

## 2026-09-01 12:55

- 根据实机截图继续修复模型文字裁切：将超分后端/模型列比例从 46%/54% 调整为 38%/62%，当前 `AnimeJaNai-V3-2x-HD-Sharp1-Compact-430K` 已完整显示，两个 `TensorRT (NVIDIA)` 也完整显示。
- 重新构建并覆盖安装实际 3FUI 插件，源文件与安装 DLL SHA-256 均为 `DE8AB0FB3C5E2D2D0F5C49127BEFBD0BE5CC827794C1FA5C9ADD05F623433AE3`；实际宿主 6.2.3 / LakeUI 5.3.0.0 启动正常。
- 静态控件门禁、CLI Python 测试 18/18、`git diff --check` 均通过；DPI 矩阵、500 帧预览压力和四宫格完整回归仍未执行，源码与记录未提交。

## 2026-09-01 12:56

- 使用包含模型列宽修正的最终 DLL 再次点击六个标签页，实时预览、模型下载、模型转换、模型导入、使用教程和超分工作台全部保持响应。

## 2026-09-01 14:19

- 仔细核对官方 LakeUI 5.3 源码后确认：`ModernTabControl` 会把绑定页放入内部透明内容面板并强制 `Dock=Fill`，而 `ModernPanel` 绝对布局仍会经过 WinForms 基类的锚点布局；超分根容器原先的 `Anchor.Right` 会反复恢复首次构建时的窄宽度，造成右半空白和页面闪烁。
- 已将超分根 `ModernPanel` 改为仅 `Anchor.Top|Left`，在宿主布局完成后由 `SyncUpscaleRootBounds` 显式同步宽度，并移除临时计时器/trace。实机普通窗口、最大化、还原和窄化采样均稳定，右侧内容已铺满。
- 重新构建并覆盖安装实际 3FUI 6.2.3 / LakeUI 5.3.0.0 插件，源 DLL 与安装 DLL SHA-256 均为 `D490EEA6CD9804E652A1EA6A57ED9BA7F231A83E7020A083433123DE43FAB6D6`；六个插件标签页依次切换保持响应，前台截图为 `%TEMP%\\3fui-final-host-foreground.png`。
- 静态旧控件扫描、CLI Python 测试 `18/18` 和 `git diff --check` 均通过。100%/125%/150% DPI、500 帧预览压力、四宫格完整交互及运行时控件树递归仍待补测；源码和记录尚未提交。

## 2026-09-01 14:26

- 使用实际 3FUI 6.2.3 / LakeUI 5.3.0.0 再次构建，`LakeUI baseline: 5.3.0.0` 门禁通过；编译器元数据变化后最新 DLL SHA-256 为 `BF14C138EBEB86ECF19F1F63589A96AC4E62ED8D439D985C3F3C860F19E44CAB`，源产物、`out` 产物和实机安装文件一致。
- 本次仅覆盖安装二进制，未改变 UI 或业务逻辑；根宽度、右半空白、页面闪烁和六页响应的实机验证记录保留在上一条等价构建记录中。
- 静态旧控件扫描、CLI Python 测试 `18/18`、`git diff --check` 和 LakeUI 5.3 构建均通过；DPI 矩阵、500 帧预览压力、四宫格完整交互及运行时控件树递归仍待补测，源码和记录尚未提交。

## 2026-09-02 16:38

- 根据实机截图确认插件外沿白线来自六个标签页根 `ModernPanel` 的 LakeUI 默认 1px 灰色边框，不是 Windows 窗口边框；页面覆盖 TabControl 内容区后该默认边框会形成完整矩形。
- 已在 `PluginPanel.vb` 统一关闭六个页面根的 `BorderColor`、`BorderSize` 和 `BorderRadius`，并以实际 3FUI 6.2.3 / LakeUI 5.3.0.0 重新构建、覆盖安装实机插件。
- 最新源 DLL 与安装 DLL SHA-256：`3FAFC169ED605C403916B2BA00CABF8FF8F7E7FB44FD5AEBC64A8F2C71ED0835`。本轮未启动宿主，待下次打开插件确认白线消失；其余 DPI、压力和四宫格回归仍待补测。

## 2026-09-02 22:25

- 按发布流程启动 1.2.0：版本源、根 README、Release Notes 和版本迭代记录已更新，1.1.3 已归档到历史版本；创建本地 `release/1.2.0` 分支。
- 本地插件/CLI 构建、旧 WinForms 控件静态门禁、Python 测试 18/18、发布门禁 5/5、Backend 更新事务 6/6 和 `git diff --check` 均通过。LakeUI 构建基线为 5.3.0.0，满足 5.1+ 要求。
- 远端 Backend 仍为 2026.08.26.1，本次仅发布 UI/渲染修复，不重复上传后端包；GitHub 和 ModelScope 认证已确认，待正式生成并上传 1.2.0 本体资产。

## 2026-09-02 22:55

- 1.2.0 已正式发布。提交 `10ed1f2 release: 1.2.0 LakeUI 5.1 migration` 已推送到 `fork/release/1.2.0` 和 `fork/main`，GitHub `v1.2.0` Release 已创建。
- ModelScope `AerithDream/VideoEnhancer-Releases` 已同步版本目录、stable.json 和发布说明；`AerithDream/VideoEnhancer-Models/Plugin/videoenhancer.exe` 已同步备用 EXE。
- 正式资产：EXE 16,882,467 bytes，SHA-256 为 `7cd35717ab2e3e268eb64a7ad7507c6655ce656906d4af18547e7a9041c6ec58`；stable.json 782 bytes，SHA-256 为 `add585dc126ca83ba8f9703fb31a9a6796d2f29157232f1c31e69be2beca25e8`。GitHub 和 ModelScope 下载内容哈希一致，版本/路径/大小回读一致。
- Backend 2026.08.26.1 逐文件审计为 0 变化，未重复上传后端包；安装器、更新器、发布门禁和本地测试均通过。DPI 矩阵、500 帧预览压力、四宫格完整交互和运行时控件树递归仍待后续补测。

## 2026-09-05 17:25

- 已拉取远端并切换到最新 `main` 基线，确认用户截图来自当前 3FUI/最新插件后，直接对照官方 LakeUI 5.6 与 FFmpegFreeUI 6.2.9 源码排查滚轮背景断层。
- 原因确认：超分滚动页 `_pageUpscale` 和其固定内容根 `_upscaleRoot` 同时直连 `ModernPanel1`，滚动移动内容根后形成两条独立背景采样/裁剪链；控件边界处因此出现宿主背景与滚动视口背景不连续。
- 修复：保留 `_pageUpscale` 的宿主背景映射，删除 `_upscaleRoot.BackgroundSource = ModernPanel1`，让内部根面板按 LakeUI 规则自动沿滚动页父级 GPU 表面取背景。没有改布局尺寸、滚动步长、控件或业务逻辑。
- 验证：官方 LakeUI 5.6、FFmpegFreeUI 6.2.9、插件编译均通过；LakeUI 自带测试通过；CLI Python `18/18` 通过；插件实例化核对页面背景源、内容根父级和 Vertical 滚动契约通过；`git diff --check` 通过。
- 待办：本轮 computer-use 原生管道不可用，尚未在真实 3FUI 中自动滚轮截图确认；建议关闭宿主后部署构建 DLL，实机确认后提交源码。版本号不变，因此未更新版本迭代记录。

## 2026-09-05 17:34

- 已确认实际 3FUI 未运行，并将修复后的 `VideoEnhancerPlugin/out/videoenhancer.dll` 覆盖到 `C:\Program portable\3FUI\plugin\videoenhancer.3fui.dll`。
- 部署回读通过：源文件与安装文件均为 4,621,312 bytes，SHA-256 为 `363420DDF7F438568246E781555D271953D6BD9E58291309805DA30D4C67EDEE`；原安装 DLL 已备份到 `C:\Users\maxzr\AppData\Local\Temp\3fui-plugin-backup-20260905-173400\videoenhancer.3fui.dll`。
- 已交给用户进行真实 3FUI 鼠标滚轮测试；视觉回归尚未宣称完成。源码与记录仍未提交，版本号不变，因此未更新版本迭代记录。

## 2026-09-05 17:56

- 用户实测确认第一版仍有明显背景断层，说明仅移除 `_upscaleRoot.BackgroundSource` 不足，不能作为最终修复。
- 重新对照官方 LakeUI 5.6：自动祖先背景采样使用 `registerDependency:=False`，滚轮改变父级坐标时不会自动让子控件 GPU 表面重绘；显式 `BackgroundSource` 才会注册来源和坐标依赖。
- 第二版在 `PluginPanel.vb` 新增递归绑定，为超分滚动内容根及其所有 LakeUI V5 子控件设置稳定的 `BackgroundSource = ModernPanel1`，已有显式来源不覆盖；未改布局、滚动步长和业务逻辑。
- 以官方 FFmpegFreeUI 6.2.9/LakeUI 5.6.0.0 构建通过并重新部署到实际 3FUI。源/目标 DLL SHA-256 均为 `A31B0CB0A0159AB1726424ED4BA6E343E3E987DBFE80E5D6B9DFA3FE6BB81828`；旧 DLL 备份位于 `%TEMP%\3fui-plugin-backup-20260905-175631\videoenhancer.3fui.dll`。
- 未启动宿主的插件对象树核对通过：超分滚动根下 76 个控件全部具有显式背景源，76 个均指向 `ModernPanel1`，没有其他来源。
- 待用户完全退出并重启 3FUI 后，在截图位置来回滚动确认；源码和状态记录尚未提交，版本号不变。

## 2026-09-05 18:18

- 用户确认滚动背景断层已修好，但发现模型选择框打开自定义菜单前短暂出现浅灰蓝色、带当前模型名称的覆盖框。
- 对照官方 LakeUI 5.6 `ModernComboBox` 确认：默认 `DropDownAnimationDuration=300ms`，而插件会在 `DropDownOpened` 中关闭原生 Overlay 后打开自定义模型菜单，导致原生选中项关闭动画短暂可见。
- 新增 `ConfigureModelSelector`，仅将放大模型和补帧模型两个自定义菜单框的原生下拉动画设为 0；其他组合框样式和动画不变。插件构建、对象核对均通过。
- 已备份旧 DLL 并将新版本部署到实际 3FUI；源/目标 SHA-256 均为 `456D3E08DABC36AFACC9B9072561860B9BB916FDFCE250EF1C7EB345CDC644E0`。备份位于 `%TEMP%\3fui-plugin-backup-20260905-181755\videoenhancer.3fui.dll`。
- 部署后的只读检查发现用户随后已启动 `FFmpegFreeUI`（PID 35744）；未强制结束进程，需退出并重新打开宿主后再确认第三版 DLL 的交互效果。
- 待用户重启 3FUI 点击模型框确认图 2 样式，并再次滚动确认背景修复未回退；源码和记录尚未提交，版本号不变。

## 2026-09-05 18:43

- 按用户要求将两个 UI 修复拆成独立提交：滚动背景依赖修复为 `c5e9be8`，模型选择框原生 Overlay 动画修复为 `fad5bff`。
- 启动 1.2.1 发布流程，统一更新插件版本源、CLI 版本、README、Release Notes 和版本迭代记录；1.2.0 已归档到历史版本。
- 使用官方 FFmpegFreeUI 6.2.9 / LakeUI 5.6.0.0 构建候选包。Backend 2026.08.26.1 逐文件审计为 0 变化；候选 EXE 16,865,524 bytes、SHA-256 `c67c8dffd05f8b52ab3ad3c657e51048c922677bca3afea89a2ad71f7a3ee005`；stable.json 518 bytes、SHA-256 `e80955ea69aba4b19dad68725c74d3f5cb961f34276117447ab40aca82730433`。
- 发布门禁 5/5、安装器 6 场景、更新器 7 场景、Backend 事务 6/6、全量 Python 测试 18/18 和 `git diff --check` 通过。候选包尚未上传，待提交/推送后进行 GitHub 与 ModelScope 双源发布及远端回读。

## 2026-09-05 18:47

- 1.2.1 发布准备提交 `bcb4e42 release: 1.2.1 UI fixes` 已推送到 `fork/main`；滚动背景修复 `c5e9be8` 与模型框 Overlay 修复 `fad5bff` 保持分别提交。
- GitHub `v1.2.1` 已创建，资产仅包含 `stable.json` 和 `VideoEnhancer-1.2.1-win-x64.exe`；ModelScope Releases 已同步版本目录、清单和说明，Models 仓库 `Plugin/videoenhancer.exe` 备用文件已同步。
- 双源回读通过：本地/GitHub/ModelScope stable.json 均为 1.2.1，EXE 为 16,865,524 bytes，SHA-256 `c67c8dffd05f8b52ab3ad3c657e51048c922677bca3afea89a2ad71f7a3ee005`；GitHub、ModelScope Releases 和 Models 备用 EXE 三处下载内容哈希一致；Release Notes 与远端正文一致。
- 发布地址：GitHub `https://github.com/maxzrb/VideoEnhancer/releases/tag/v1.2.1`；ModelScope Releases `https://www.modelscope.cn/datasets/AerithDream/VideoEnhancer-Releases`；Models 备用路径 `Plugin/videoenhancer.exe`。
- 后续仅需用户完全重启当前 3FUI，实测模型菜单不再闪出浅灰蓝色模型条，并确认滚动背景未回退；DPI、500 帧压力和四宫格完整交互仍为后续回归项。

## 2026-09-12 09:55

- 启动 1.3.0 本地开发：新增 RTX VSR、RTX Video HDR、目标分辨率/倍率映射和能力检查；RTX VSR 与补帧固定先补帧，HDR 放在最后处理，已是 PQ/HLG 的素材会拒绝重复 HDR 映射。
- 插件首页新增 HDR 区和 RTX 专用输出规格/质量控件；图片区新增“移除所有”；顶部新增“右键超分”，可配置“超分辨率 → 模型”级联菜单并固定输出 PNG。
- FlashVSR 与 BasicVSR++ 已能处理图片：后端构造重复单帧无损视频，完成时序超分后提取第一帧；失败信息现在会指出图片、阶段、退出码和后端日志，小图会自动补边后裁回目标尺寸。
- RTX 4070 Laptop 实测通过：RTX VSR 320×180→640×360；RTX HDR 输出 HEVC 10-bit BT.2020/PQ；BasicVSR++ 图片 64×48→256×192；FlashVSR 图片 64×64→256×256。LakeUI 5.9 / FFmpegFreeUI 6.2.16 插件构建和 CLI 构建通过。
- 本轮按要求未执行任何 Git 命令，未提交、推送或发布；RTX SDK 文件公开再分发前仍需复核 NVIDIA 许可。下一步完成最终 1.3.0 构建、部署和真实 3FUI 视觉检查。

## 2026-09-12 10:03

- 修正“右键超分”页初始化期间可能在控件句柄创建前调用 `BeginInvoke` 的风险；模型列表只在用户切换到该页时异步加载。使用 LakeUI 5.9 / FFmpegFreeUI 6.2.16 重新构建插件成功。
- 最终 1.3.0 已部署到 `C:\PortableSoft\FFmpegFreeUI ReadyToRun x64`：EXE 16,906,285 bytes、SHA-256 `F687EBFAB5C1BE65BCE3159D80B86782B6D675632F519DC66A5324AD721FD713`；DLL 4,632,576 bytes、SHA-256 `27D130D0F89BBDCFA1C16526C5B2F1804B388DE5449FD680BB1C920AA1BBE425`。源文件与安装文件哈希一致，覆盖前备份位于 `%LocalAppData%\Temp\videoenhancer-1.3.0-backup-20260912-095800`。
- 最终回归：CLI Release 构建 0 错误（2 个既有 CA1416 警告）、Python 测试 18/18、安装版版本号 1.3.0、RTX VSR / RTX Video HDR 环境探测全部通过；用户手动确认真实 3FUI 新界面无问题。
- 未代替用户点击“应用设置”，因此资源管理器右键菜单仍由用户按需启用。本轮没有执行任何 Git 命令，也没有提交、推送或发布；RTX SDK 文件公开发行前仍需复核 NVIDIA 许可。

## 2026-09-12 10:47

- HDR“处理方式”下拉已改为可点击；“右键超分”模型列表支持逐项删除，并保留清空、应用设置和移除菜单操作。
- 顶栏新增“分段超分”：读取 3FUI 当前视频列表及精确帧数，每个视频自动保存总开关和分段；分段首尾锁定、内部边界联动，必须无重复、无缺口覆盖全部帧。只列 NCNN/CUDA/TensorRT/ONNX 单帧算法，第一段选择后锁定后端类别与倍率，后续只可选择兼容模型。
- 新增逐帧分发后端：单次 FFmpeg 解码，不同分段可切换不同模型，最后由同一 FFmpeg 编码并保留音频/字幕。分段模式当前不与运动补帧、RTX HDR 或 PQ/HLG 输入组合。
- 可用性实测通过：两帧视频分别用两个 NCNN 2x 模型处理，源码、trimmed publish 和安装版 EXE 均输出 256×256、2 帧；重复/缺口边界和混合倍率会明确拒绝。CLI/插件构建通过，Python 测试 22/22。
- 用户退出 3FUI 后已部署最终产物。EXE SHA-256 为 `2E75BF83CEFF8B24A9228EBD61876923E7C30AF4FE4D8B417006C53726B0C9A8`，DLL 为 `9F1BF2328BACAB8A7A86C7F3318BD3543DBB0655BE71C348F86785B9AF889FCD`，分段后端为 `2B972FDC115518EE8C3BCCD0552E23DDD062F3E6AA00A2D39E07948BB9BF9C41`；源码与安装目录一致，安装版版本仍为 1.3.0。未启动宿主，新增页面待用户下次打开后目视确认。
- 本轮没有执行任何 Git 命令，也没有提交、推送或发布。覆盖前备份位于 `%LocalAppData%\Temp\videoenhancer-1.3.0-segment-backup-20260912-103000`。

## 2026-09-12 12:55

- 确认仓库拓扑：`user-Wing/VideoEnhancer` 是父仓库，`maxzrb/VideoEnhancer` 是主要维护 fork；本地开发基于 `maxzrb/main`。
- 1.3.0 功能提交为 `9551296 feat: add RTX and segmented upscale workflows`，已推送到 `user-Wing/VideoEnhancer:feat/rtx-segmented-upscale-1.3.0`，并向 `maxzrb/main` 创建 PR #1：`https://github.com/maxzrb/VideoEnhancer/pull/1`。
- 同步前两边 main 已分叉。没有强推覆盖 user-Wing，而是以 `9551296` 和旧 `user-Wing/main` 为双父创建合并提交 `becc929`；合并树保持 maxzrb 维护线的完整 1.3.0 内容，user-Wing 的 5 个独有提交仍保留在历史中。`user-Wing/main` 已从 `cbfda2f` 快进到该合并提交。
- 提交范围只包含本次 18 个源码、测试和文档文件，不含 EXE、DLL、INI、测试缓存或临时素材。版本仍为 1.3.0，未创建标签、Release 或发布资产。
