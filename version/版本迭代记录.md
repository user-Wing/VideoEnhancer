# 版本迭代记录

用途：面向中文用户记录版本发布、重要变更、影响范围、验证结果和迁移说明。仅当项目版本或发布状态变化时更新。

## 当前版本

- 独立版本：1.3.0
- 发布日期：未发布
- 状态：本地开发、部署与实机验证完成；源码已直接更新到 `user-Wing/main`，并向主要维护仓库 `maxzrb/VideoEnhancer` 提交 PR #1；未创建标签、Release 或发布资产。
- 主要变化：新增 NVIDIA RTX VSR 与 RTX Video HDR；RTX VSR 支持倍率和目标分辨率映射，组合补帧时固定先补帧；新增 Windows 图片级联右键超分设置页及逐项删除；FlashVSR 与 BasicVSR++ 新增图片单帧视频桥；图片输入栏新增“移除所有”；新增按视频保存、完整帧覆盖并锁定后端/倍率的分段超分。
- 影响范围：CLI RTX sidecar 协议、视频组合与分段逐帧管线、图片后端、插件工作台布局、队列参数、插件配置和当前用户资源管理器右键菜单。
- 验证情况：LakeUI 5.9 / FFmpegFreeUI 6.2.16 插件构建通过；CLI Release 构建 0 错误（2 个既有 Windows 平台分析警告）；Python 测试 22/22；安装版版本号和 RTX 环境检查通过；RTX 4070 Laptop 上 RTX VSR 320×180→640×360、RTX HDR HEVC Main10 BT.2020/PQ、BasicVSR++ 图片 64×48→256×192、FlashVSR 图片 64×64→256×256 均已实跑通过；分段管线用两个不同 NCNN 模型逐帧处理并成功输出 256×256、2 帧视频，重复/缺口边界及混合倍率均被拒绝；用户已确认此前真实 3FUI 界面无问题，新增分段页待重启后目视确认。
- 已知风险：RTX Video 运行组件包含 NVIDIA 专有 SDK 文件，公开再分发前必须复核其许可；右键菜单只在用户点击“应用设置”后写入当前用户注册表；分段超分当前不与运动补帧、RTX HDR 或 PQ/HLG HDR 输入组合；maxzrb PR #1 尚待维护者审查合并。
- 迁移说明：本地 1.2.x 配置可直接加载；RTX 功能需在 `plugin\videoenhancer\bin\rtx-video` 提供 sidecar 及同目录运行库。

## 历史版本

发布新版本时，先把旧的“当前版本”内容移到本节顶部，再写入新的“当前版本”。不要删除历史记录。

### 1.2.1（2026-09-05，已退役）

- 状态：已正式发布 GitHub `v1.2.1`，并同步 ModelScope Releases 稳定通道和 Models 备用 EXE；后续由 1.3.0 开发线替代。
- 主要变化：按官方 LakeUI 背景依赖注册语义修复滚动页面控件与背景之间的断层；关闭放大模型和补帧模型自定义菜单切换时不需要的原生 Overlay 动画。
- 影响范围：插件超分页面的滚动 GPU 背景刷新和放大/补帧模型选择交互。
- 验证情况：EXE 16,865,524 bytes，SHA-256 `c67c8dffd05f8b52ab3ad3c657e51048c922677bca3afea89a2ad71f7a3ee005`；LakeUI 5.6 插件、CLI、发布门禁、安装器、更新器、Backend 事务和 Python 测试均通过，双源资产回读一致。
- 迁移说明：可直接升级到后续版本，Backend 2026.08.26.1 无需重复下载。

### 1.2.0（2026-09-02，已退役）

- 状态：已正式发布 GitHub `v1.2.0`，并同步 ModelScope Releases 稳定通道和 Models 备用 EXE；后续由 1.2.1 替代。
- 主要变化：完成六页主面板、模型能力编辑器和四宫格窗口的 LakeUI 5.1+ 原生控件迁移；修复透明 GPU 表面的黑块、文字重叠、裁切、右半空白、页面闪烁和页面根边框；修复下拉框首字偏移与模型名称显示不完整；加入 LakeUI 5.1+ 启动兼容门禁。
- 影响范围：插件 UI 渲染、页面布局、模型菜单、教程、模型编辑器和四宫格预览；视频处理、模型、配置、队列钩子和 CLI 参数保持不变。
- 验证情况：EXE 16,882,467 bytes，SHA-256 为 `7cd35717ab2e3e268eb64a7ad7507c6655ce656906d4af18547e7a9041c6ec58`；stable.json 782 bytes，SHA-256 为 `add585dc126ca83ba8f9703fb31a9a6796d2f29157232f1c31e69be2beca25e8`；Backend 2026.08.26.1 逐文件审计为 0 变化；CLI/插件构建、旧控件静态门禁、Python 测试 18/18、发布门禁 5/5、安装器和更新器隔离测试已通过；GitHub Release、ModelScope stable.json、发布包和 Models 备用 EXE 的版本/路径/大小/哈希已回读一致。
- 已知风险：全新 Windows 10 仍需 Microsoft VC++ 2015–2022 x64 运行库；CUDA 13.0 后端建议 NVIDIA 580 或更高驱动；RealHatGAN 1080p 仍可能需要较小分块；DPI 矩阵、500 帧预览压力和四宫格完整鼠标流程仍需后续实机补测。
- 迁移说明：可由 1.1.3 直接自动更新；Backend 保持 2026.08.26.1，不需要重复下载后端大包。

### 1.1.3（2026-08-27，已退役）

- 状态：已正式发布；后续由 1.2.0 替代。
- 主要变化：用户模型支持 Delete 键和右键删除菜单；内置模型菜单改为按具体模型/变体提供悬浮说明；关闭状态的选项下拉框不再响应鼠标滚轮；使用教程改为面向初学者的逐步说明。
- 影响范围：模型导入与删除、模型菜单提示、选项下拉框交互、使用教程和插件文档。
- 验证情况：EXE 16,877,609 bytes，SHA-256 为 `16e1728a99a2ed568909bdc44df1c7df7cea7356a4135e7661e57a127859bf50`；stable.json 为 673 bytes，SHA-256 为 `0da8e3cb5e49a62b199c048a3ecac41f36d38db539c9cfdffa29c25ad3060276`；Backend 2026.08.26.1 逐文件审计为 0 变化；CLI/插件构建、全量 Python 测试 18/18、发布门禁 5/5、安装器和更新器隔离测试已通过；GitHub Release、ModelScope stable.json、发布包和 Models 备用 EXE 回读一致。
- 已知风险：全新 Windows 10 仍需 Microsoft VC++ 2015–2022 x64 运行库；CUDA 13.0 后端建议 NVIDIA 580 或更高驱动；RealHatGAN 1080p 仍可能需要较小分块。
- 迁移说明：可由 1.1.2 直接自动更新；Backend 保持 2026.08.26.1，不需要重复下载后端大包。

### 1.1.2（2026-08-26，已退役）

- 状态：已正式发布；后续由 1.1.3 替代。
- 主要变化：超分和补帧分别提供默认启用的半精度推理开关；同后端混合精度使用独立参数且保持单进程；环境检查补充 Windows 10、VC++、CUDA/PyTorch、TensorRT、ONNX 和 NCNN 依赖诊断。
- 影响范围：视频任务队列、图片增强、超分/补帧精度、环境检查和安装文档。
- 验证情况：CLI/插件构建、六类后端依赖检查、安装器五场景、更新器七场景、Backend 事务 6/6、发布门禁 5/5、有序管线 8/8 和 RTX 3060 短样本组合测试通过；GitHub 与 ModelScope EXE 均为 16,878,865 bytes，SHA-256 为 `da4bbd96d2f21792c1b85abb1c185e2366e766b7131d8e8f2cc2379abb754605`。
- 已知风险：全新 Windows 10 仍需 Microsoft VC++ 2015–2022 x64 运行库；CUDA 13.0 后端建议 NVIDIA 580 或更高驱动；RealHatGAN 1080p 仍可能需要较小分块。
- 迁移说明：可由 1.1.1 直接自动更新；Backend 保持 2026.08.26.1，不需要重复下载后端大包。

### 1.1.1（2026-08-26，已退役）

- 状态：已正式发布；后续由 1.1.2 替代。
- 主要变化：新增用户模型导入、元数据安全预检、能力修正和 `models\User` 事务安装；内置模型能力清单与 LakeUI 二级模型菜单；修复尺寸敏感模型、ONNX 分块和更新重启确认流程；移除低使用率的对比工具和独立模型指南。
- 验证情况：CLI/插件构建、模型与协议测试、安装器、自动更新器、Backend 事务、顺序管线和发布门禁均通过；最终资产已在 GitHub 与 ModelScope 回读核验。
- 迁移说明：可直接自动更新到 1.1.2，Backend 版本保持 2026.08.26.1。

### 1.1.0（2026-08-25，已退役）

- 状态：已正式发布；因发布时间很短，按用户授权在不升版本号的前提下覆盖一次界面对齐和模型致谢修复。
- 主要变化：插件 DLL 保留在 `Plugin` 根目录；EXE、Backend、模型、FFmpeg 与运行状态迁入 `Plugin\videoenhancer`；全新安装自动处理版本化 EXE 文件名；旧版自动更新使用可恢复的布局迁移事务；模型下载页最新状态恢复垂直居中，README 增补当前全部模型家族的来源与致谢。
- 影响范围：全新安装、自动更新、插件路径解析、模型与 Backend 路径、发布门禁及 GPU 兼容矩阵。
- 验证情况：真实 3FUI 已迁移 47,676 个受管文件、24,989,634,005 字节，迁移前后 SHA-256 清单 0 缺失、0 新增、0 变化；插件页、嵌套路径、自检和自动重启通过。679 项五路 GPU 矩阵最终 677 PASS、2 项 FlashVSR+GIMM 因 6GB 显存不足跳过、0 失败；安装器、更新器、Backend 事务、顺序管线和发布门禁全部通过。
- 已知风险：FlashVSR+GIMM 两种顺序在 6GB RTX 3060 上会 OOM，已按测试约定记录为资源不足组合；Backend 2026.08.25.1 无发布变化。
- 迁移说明：可直接自动更新到 1.1.1。

### 1.0.11（2026-08-25，已退役）

- 状态：为 Backend 增量补丁不适用场景补齐用户可选完整修复入口。
- 主要变化：补丁 SHA-256 冲突或健康检查失败回滚后显示“下载完整修复包”；用户确认后跳过增量补丁并事务安装完整后端；状态接口同时返回完整包大小。
- 验证情况：CLI/插件构建和单文件发布通过；Backend 事务测试 6/6；GitHub 与 ModelScope 三处 EXE 大小和 SHA-256 一致。
- 已知风险：GitHub 版本化 EXE 需要手动改名才能被旧布局识别；已由 1.1.0 的自动子目录安装修复。
- 迁移说明：可直接自动更新到 1.1.0；Backend 版本保持 2026.08.25.1。

### 1.0.10（2026-08-25，已退役）

- 状态：修复启动环境自检占用后端、阻止 Backend 增量更新的问题。
- 主要变化：启动环境检查不再逐个加载 TensorRT Engine；本体或 Backend 更新前自动终止插件自身的自检进程树。
- 验证情况：CLI/插件构建、顺序处理、自更新、Backend 更新和发布门禁通过；三处发布 EXE 哈希一致。
- 已知风险：增量补丁因本地文件哈希不一致而回滚后，UI 只显示“重试”，没有完整修复入口；已由 1.0.11 修复。
- 迁移说明：可直接自动更新到 1.0.11，Backend 无需主动重复下载。

### 1.0.9（2026-08-25，已退役）

- 状态：作为 1.0.8 自更新重启问题的修正版，并正式启用 Backend 文件级增量更新通道。
- 主要变化：目标 EXE/DLL 短暂占用时自动重试；宿主已退出后无论更新成功或失败都恢复启动 3FUI；发布 Backend 2026.08.25.1 完整包、7 文件增量包和 channel。
- 验证情况：更新器 6 类隔离场景通过；后端完整包、生产补丁、远端哈希和客户端增量选择均通过。
- 已知风险：启动时 TensorRT 环境自检会加载全部 Engine，可能占用后端并阻止增量更新；已由 1.0.10 修复。
- 迁移说明：可直接自动更新到 1.0.10，Backend 无需重复下载。

### 1.0.8（2026-08-25，已退役）

- 状态：首个包含 Backend 文件级增量更新客户端的版本；发布时按用户要求暂缓了后端资产和通道。
- 主要变化：修复多后端超分/补帧组合与模型识别问题；新增事务式后端文件级增量更新、回滚和严格发布审计。
- 验证情况：RTX 3060 代表模型矩阵 578 项取得终态，576 项通过、2 项因 6 GB 显存不足跳过；后端更新隔离测试 6/6、发布门禁 5/5 通过。
- 已知风险：目标 EXE 在宿主退出后短暂被占用时更新会失败，且失败后不会恢复启动 3FUI；已由 1.0.9 修复。
- 迁移说明：手动重新打开 3FUI 后可直接更新到 1.0.9。

### 1.0.7（2026-08-24，已退役）

- 状态：1.0 系正式测试版本；本体采用单 EXE 发布，GitHub Release 为版本检查与下载首选，ModelScope 为网络受限时兜底。
- 主要变化：沿用 1.0.6 引入的 EXE-only 更新协议，并以 1.0.7 作为首个从已安装 1.0.6 自动升级的双源测试版本；新 EXE 作为临时更新器替换目标 EXE并释放 DLL，失败回滚。
- 影响范围：CLI 自更新协议、插件更新启动方式、发布/测试脚本、README 和发布门禁、GitHub Release `v1.0.7`、两个 ModelScope 数据集。
- 验证情况：CLI/插件构建通过；EXE-only 更新器正常更新、篡改拒绝、无效资产拒绝和 DLL 锁定回滚隔离测试通过。
- 已知风险：1.0.5 及更早版本不能直接使用旧 ZIP 更新器安装 EXE-only 版本。
- 迁移说明：可直接自动更新到 1.0.8。

### 1.0.6（2026-08-24，已退役）

- 状态：首个 EXE-only 协议版本，作为 1.0.7 自动更新测试的本地起点。
- 主要变化：Release 只发布内嵌插件 DLL 的 EXE；更新时替换 EXE并释放 DLL。
- 验证情况：EXE-only 更新器正常更新、篡改拒绝、无效资产拒绝和 DLL 锁定回滚通过。
- 迁移说明：可通过新协议直接自动更新到 1.0.7。

### 1.0.5（2026-08-24，已退役）

- 状态：最后一个使用三文件 ZIP 更新协议的版本，现由 1.0.6 替代。
- 主要变化：修复“下载全部”重复插件 EXE；更新检查和下载改为 GitHub 首选、ModelScope 兜底。
- 验证情况：CLI/插件构建和旧 ZIP 更新器隔离测试通过。
- 迁移说明：需手动下载并运行一次 1.0.6 EXE，之后恢复自动更新。

### 1.0.4（2026-08-24，已退役）

- 状态：独立 1.0 系正式测试版本，现由 1.0.5 替代。
- 主要变化：修复环境检查可选资源误报；模型下载页将“下载插件更新”改为“下载全部”，并把 `Plugin/videoenhancer.exe` 纳入资源列表。
- 验证情况：CLI/插件构建和更新器隔离测试通过；GitHub 与 ModelScope 清单均指向 1.0.4。
- 迁移说明：可直接从插件检查更新升级到 1.0.5。

### 1.0.3（2026-08-24，已退役）

- 状态：独立版本编号重置后的首个版本，现由 1.0.4 替代。
- 主要变化：完成 GitHub 唯一版本检查、ModelScope 首选/GitHub 兜底下载和 TensorRT/RIFE 缓存机制；保留旧版 UI 和模型兼容路径。
- 验证情况：构建、发布包和更新器隔离测试通过；后续发现环境检查会将当前 GPU 不兼容的预置 Engine 误报为基础环境缺失。
- 迁移说明：可直接从插件底部检查更新升级到 1.0.4。

### 1.11.3（2026-08-23，已退役，编号重置前末版）

- 状态：版本检查切换 GitHub 权威标准 + 双源下载的版本，功能由 1.0.3 原样继承，仅编号被重置取代。
- 主要变化：CLI 版本号单一来源（运行时读 csproj，新增 `--version`）；插件更新检查改为 GitHub `maxzrb/VideoEnhancer` 的 `releases/latest` 并交叉校验标签与清单；更新包下载双源（ModelScope 首选、GitHub 回退，两源均校验大小与 SHA-256）；发布脚本一键上传；更新按钮移到最右列、更新弹窗不再显示更新内容。
- 影响范围：CLI 版本机制、插件更新器、底部状态栏与模型下载页布局、发布脚本、GitHub fork、ModelScope Releases 数据集。
- 验证情况：实机验证 GitHub 检查发现、GitHub 兜底下载、真实替换与自替换冲突回滚全部通过。
- 已知风险：ModelScope 读缓存当晚延迟未追平（写入已确认）。
- 迁移说明：手动升级到 1.0.3（或任意 1.0 系版本）。

### 1.11.2（2026-08-23，已退役）

- 状态：简化更新确认窗口文案的过渡版本，现由 1.11.3 替代。
- 主要变化：重新整理更新确认窗口，只显示新版本、当前版本、更新包大小、简洁更新内容和重启说明；不再向普通用户显示上游基线或开发式自我说明。
- 影响范围：插件自动更新确认窗口、ModelScope 1.11.2 稳定清单和发布包。
- 验证情况：CLI Release 与插件编译通过；修正文案的过渡 1.11.0 DLL 已单独构建，更新事务隔离测试继续通过。
- 已知风险：真实 3FUI 内的新确认窗口和退出/自动重启仍需本次升级实测；GIMM-VFI、GMFSS 和 TensorRT 仍待 NVIDIA 环境实测。
- 迁移说明：1.11.0/1.11.1 用户均可从底部或模型下载页入口直接升级到 1.11.2。

### 1.11.1（2026-08-23，已退役）

- 状态：增加模型下载页更新兜底入口的测试版本，现由 1.11.2 替代。
- 主要变化：模型下载页新增“下载插件更新”，复用稳定通道的完整检查、校验、回滚和重启流程。
- 影响范围：插件模型下载页标题栏、ModelScope 稳定清单和发布包。
- 验证情况：过渡 1.11.0 插件成功发现并下载校验 1.11.1；更新器隔离测试通过。
- 已知风险：更新确认窗口显示了不必要的上游基线，且发布说明采用开发式自我指涉文案，用户体验不自然。
- 迁移说明：直接升级到 1.11.2。

### 1.11.0（2026-08-23，已退役）

- 状态：首个使用独立 SemVer 和 ModelScope 稳定更新通道的正式版本，现由 1.11.1 替代。
- 主要变化：独立版本与上游基线分离；增加 ModelScope 后台/手动检查、用户确认、外层与逐文件 SHA-256、退出后替换、失败回滚和自动重启；选择性接入上游 1.4.2 的新版模型能力。
- 影响范围：CLI 外部更新模式、插件启动与底部状态栏、ModelScope Release 数据集、版本和发布脚本。
- 验证情况：编译、公开远端读取、正常更新、路径穿越拒绝、篡改拒绝、外层错误哈希拒绝和中途失败回滚均通过。
- 已知风险：模型下载页没有独立更新入口，底部检查更新按钮是唯一手动入口。
- 迁移说明：直接升级到 1.11.1；配置格式不变。

### 1.10.1（2026-08-23，已退役）

- 状态：曾作为独立维护正式主线，现由 1.11.0 替代。
- 主要变化：默认模型源切换到 `AerithDream/VideoEnhancer-Models`；正式移除 preview 标识；环境检查只严格验证当前选择的后端，并优先显示真实缺失项。
- 影响范围：CLI 版本信息、ModelScope 模型清单与下载、后端环境检查、插件启动状态提示。
- 验证情况：CLI 与插件编译通过；NCNN 配置检测 22 个模型并全部通过；无 CUDA 环境中的显式 TensorRT 检查正确失败；公开 ModelScope 模型下载与 SHA-256 校验通过。
- 已知风险：没有内置插件更新通道，发行仍依赖手动替换文件。
- 迁移说明：直接升级到 1.11.0；原配置可继续使用。

### 1.9.6-preview.2（2026-08-22，已退役）

- 状态：曾作为抢先体验主线，现由正式主线 1.10.1 替代。
- 主要变化：TensorRT PTH 首次使用自动构建 Engine，并按 GPU、TensorRT 版本、输入尺寸和源模型摘要隔离缓存；超分与补帧可同时开启；修复最小化恢复时的背景穿透与分块重绘路径。
- 影响范围：CLI TensorRT 模型解析/缓存、视频增强执行管线、插件超分工作台配置和 3FUI 窗口恢复绘制。
- 验证情况：CLI 与插件编译通过；TensorRT 伪后端首次构建/缓存命中、组合管线、FFV1 编码、插件布局/恢复状态和 ZIP 解压复验均通过。
- 已知风险：启动环境检查会验证目录内无关 TensorRT Engine，且插件失败提示只显示第一行版本信息；这些问题已在 1.10.1 修复。
- 迁移说明：直接升级到 1.10.1；原有配置可继续使用，但无 CUDA 机器需切换为 NCNN/ONNX 等后端。

### 1.9.6-preview.1（2026-08-22，已退役）

- 状态：曾作为当前开发主线，现由 preview.2 替代。
- 主要变化：采用原作者远端 `220ebb4`（UI 贡献 `a95bdfe`）的新界面；保留并适配 ModelScope 在线列表顺序、真实网络异常识别、超时和错误详情修复。
- 影响范围：3FUI 插件整体界面、模型下载页，以及 CLI 在线模型列表和下载错误处理。
- 验证情况：CLI 与插件编译通过；EXE 报告 v1.9.6-preview.1；在线列表返回 82 项；ZIP 清单和 SHA-256 已检查。
- 已知风险：毛玻璃恢复分块重绘、TensorRT 仅消费预制 Engine、超分和补帧界面互斥；这些问题已在 preview.2 处理。
- 迁移说明：直接升级到 preview.2；原有配置可继续使用。

### 1.9.5（2026-08-22，已退役）

- 状态：已构建，待用户实际 3FUI 环境回归。
- 主要变化：全局可见控件统一为 LakeUI；开关恢复标准比例；按钮统一为 34px 高；修复 ModelScope 在线列表被本地 `core-path` 错误阻断并误报断网的问题。
- 影响范围：3FUI 插件主页面、图片超分页、高级功能页、模型下载页、模型转换页、视频对比工作室，以及 CLI 在线模型列表/下载错误处理。
- 验证情况：CLI 与插件编译通过；在线列表返回 82 项；ModelScope API 和文件直链均返回 HTTP 200。
- 已知风险：未在用户实际宿主中完成多 DPI 视觉回归；未下载大型模型文件。
- 迁移说明：备份并替换运行目录中的 `videoenhancer.exe` 与插件目录中的 `videoenhancer.3fui.dll`，重启 3FUI；配置文件格式无需迁移。
